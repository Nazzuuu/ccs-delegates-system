import {
  DndContext,
  DragOverlay,
  PointerSensor,
  pointerWithin,
  useDndContext,
  useDraggable,
  useDroppable,
  useSensor,
  useSensors
} from "./chunk-GY65XFZI.js";
import {
  Drawer,
  formatBytes,
  getFileExtension,
  getTranslationKey,
  prefixFileUrlWithBackendUrl,
  uploadApi,
  useUploadFileSilentlyMutation,
  useUploadFilesMutation,
  useUploadFromUrlsMutation,
  validateUrls
} from "./chunk-66LJOAF3.js";
import {
  ASSET_TYPES
} from "./chunk-7GZQ6DOF.js";
import "./chunk-ZKKX5PUT.js";
import "./chunk-XS6TXKIG.js";
import "./chunk-FIKB3MFG.js";
import "./chunk-FCJ27RU7.js";
import "./chunk-4OHSMTUC.js";
import "./chunk-K33U75AR.js";
import "./chunk-CIKRVPYW.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-JNUQ3FRY.js";
import "./chunk-5Z4OCUJA.js";
import "./chunk-JF2YXGDV.js";
import "./chunk-MU3XTMFL.js";
import {
  useClipboard
} from "./chunk-PUO4Z3RA.js";
import {
  usePersistentState
} from "./chunk-UHTS5LIN.js";
import "./chunk-TXQWTJEO.js";
import "./chunk-TINWRCGC.js";
import "./chunk-7MO7L32J.js";
import "./chunk-3F4HWONF.js";
import "./chunk-PZ35V56S.js";
import "./chunk-HHPZYJCU.js";
import "./chunk-56LACPBQ.js";
import "./chunk-JRCQYP7F.js";
import "./chunk-ZJCTND2Z.js";
import "./chunk-45QSVGD5.js";
import "./chunk-4ITQAMY7.js";
import "./chunk-HTW3RZJT.js";
import "./chunk-MBK4V2X7.js";
import "./chunk-IFOFBKTA.js";
import "./chunk-2H5CDXMF.js";
import "./chunk-A6QRMII5.js";
import "./chunk-2IRG5XKX.js";
import "./chunk-V75JRYI6.js";
import {
  getDisplayName
} from "./chunk-YFV7NR6Y.js";
import "./chunk-FJROOSKM.js";
import {
  Blocker,
  Form,
  useField,
  useForm
} from "./chunk-WPM7JWMA.js";
import "./chunk-YQ5LRYDY.js";
import "./chunk-KI4SASRC.js";
import "./chunk-QKQCKBET.js";
import {
  Layouts,
  useElementOnScreen
} from "./chunk-5U5EFMAX.js";
import {
  useIsMobile
} from "./chunk-VHCUWX2O.js";
import "./chunk-L4VZK4CF.js";
import "./chunk-3ED3YA5Q.js";
import "./chunk-4GG6X3TE.js";
import "./chunk-KVWVHPCR.js";
import {
  Page
} from "./chunk-VCZQ2XNI.js";
import {
  useQueryParams
} from "./chunk-EWBGXVVO.js";
import {
  useNotification
} from "./chunk-YPRZDW5I.js";
import "./chunk-DQ3G2KUY.js";
import "./chunk-ALJOKJHY.js";
import "./chunk-LHCRHSEP.js";
import "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-FZKXTSGW.js";
import "./chunk-HAUWLGB7.js";
import "./chunk-3MST3H25.js";
import {
  Route,
  Routes
} from "./chunk-25J5DAB7.js";
import "./chunk-AHJOALQ2.js";
import "./chunk-U5ASKYVL.js";
import "./chunk-QQEUXDAQ.js";
import "./chunk-LHXIEHKN.js";
import {
  $e,
  $u,
  A,
  Cd,
  Ct,
  Dd,
  Dn,
  Hl,
  Ld,
  Mt,
  Pc,
  Pd,
  S,
  Ud,
  Ul,
  Vl,
  _d,
  _l,
  be,
  cr,
  dr,
  jl,
  kt,
  md,
  pd,
  ti,
  ud,
  un,
  v,
  xd,
  yu
} from "./chunk-MDXICPND.js";
import "./chunk-RJN7SHNB.js";
import "./chunk-PC7RLLVI.js";
import {
  B3,
  In,
  J5,
  Jn,
  K5,
  N2,
  N5,
  O2,
  Q5,
  R3,
  S5,
  W2,
  W3,
  X5,
  Z2,
  Z5,
  _5,
  d3,
  g3,
  j5,
  o5,
  r3,
  t3,
  wn,
  z5
} from "./chunk-GHLSOEY5.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  ft,
  gt,
  st
} from "./chunk-3STVGYQD.js";
import "./chunk-4MZT7456.js";
import "./chunk-QBO3FZHQ.js";
import {
  require_react_dom
} from "./chunk-KKBG5U5K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var import_react12 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var import_react11 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle-group/dist/index.mjs
var import_react3 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-context/dist/index.mjs
var React = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function createContextScope(scopeName, createContextScopeDeps = []) {
  let defaultContexts = [];
  function createContext32(rootComponentName, defaultContext) {
    const BaseContext = React.createContext(defaultContext);
    const index = defaultContexts.length;
    defaultContexts = [...defaultContexts, defaultContext];
    const Provider = (props) => {
      var _a;
      const { scope, children, ...context } = props;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const value = React.useMemo(() => context, Object.values(context));
      return (0, import_jsx_runtime.jsx)(Context.Provider, { value, children });
    };
    Provider.displayName = rootComponentName + "Provider";
    function useContext22(consumerName, scope) {
      var _a;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const context = React.useContext(Context);
      if (context) return context;
      if (defaultContext !== void 0) return defaultContext;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    return [Provider, useContext22];
  }
  const createScope = () => {
    const scopeContexts = defaultContexts.map((defaultContext) => {
      return React.createContext(defaultContext);
    });
    return function useScope(scope) {
      const contexts = (scope == null ? void 0 : scope[scopeName]) || scopeContexts;
      return React.useMemo(
        () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
        [scope, contexts]
      );
    };
  };
  createScope.scopeName = scopeName;
  return [createContext32, composeContextScopes(createScope, ...createContextScopeDeps)];
}
function composeContextScopes(...scopes) {
  const baseScope = scopes[0];
  if (scopes.length === 1) return baseScope;
  const createScope = () => {
    const scopeHooks = scopes.map((createScope2) => ({
      useScope: createScope2(),
      scopeName: createScope2.scopeName
    }));
    return function useComposedScopes(overrideScopes) {
      const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
        const scopeProps = useScope(overrideScopes);
        const currentScope = scopeProps[`__scope${scopeName}`];
        return { ...nextScopes2, ...currentScope };
      }, {});
      return React.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
    };
  };
  createScope.scopeName = baseScope.scopeName;
  return createScope;
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-primitive/dist/index.mjs
var React4 = __toESM(require_react(), 1);
var ReactDOM = __toESM(require_react_dom(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-slot/dist/index.mjs
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-compose-refs/dist/index.mjs
var React2 = __toESM(require_react(), 1);
function setRef(ref, value) {
  if (typeof ref === "function") {
    return ref(value);
  } else if (ref !== null && ref !== void 0) {
    ref.current = value;
  }
}
function composeRefs(...refs) {
  return (node) => {
    let hasCleanup = false;
    const cleanups = refs.map((ref) => {
      const cleanup = setRef(ref, node);
      if (!hasCleanup && typeof cleanup == "function") {
        hasCleanup = true;
      }
      return cleanup;
    });
    if (hasCleanup) {
      return () => {
        for (let i = 0; i < cleanups.length; i++) {
          const cleanup = cleanups[i];
          if (typeof cleanup == "function") {
            cleanup();
          } else {
            setRef(refs[i], null);
          }
        }
      };
    }
  };
}
function useComposedRefs(...refs) {
  return React2.useCallback(composeRefs(...refs), refs);
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-slot/dist/index.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
function createSlot(ownerName) {
  const SlotClone = createSlotClone(ownerName);
  const Slot2 = React3.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = React3.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (React3.Children.count(newElement) > 1) return React3.Children.only(null);
          return React3.isValidElement(newElement) ? newElement.props.children : null;
        } else {
          return child;
        }
      });
      return (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React3.isValidElement(newElement) ? React3.cloneElement(newElement, void 0, newChildren) : null });
    }
    return (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
  });
  Slot2.displayName = `${ownerName}.Slot`;
  return Slot2;
}
var Slot = createSlot("Slot");
function createSlotClone(ownerName) {
  const SlotClone = React3.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (React3.isValidElement(children)) {
      const childrenRef = getElementRef(children);
      const props2 = mergeProps(slotProps, children.props);
      if (children.type !== React3.Fragment) {
        props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      }
      return React3.cloneElement(children, props2);
    }
    return React3.Children.count(children) > 1 ? React3.Children.only(null) : null;
  });
  SlotClone.displayName = `${ownerName}.SlotClone`;
  return SlotClone;
}
var SLOTTABLE_IDENTIFIER = Symbol("radix.slottable");
function createSlottable(ownerName) {
  const Slottable2 = ({ children }) => {
    return (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, { children });
  };
  Slottable2.displayName = `${ownerName}.Slottable`;
  Slottable2.__radixId = SLOTTABLE_IDENTIFIER;
  return Slottable2;
}
var Slottable = createSlottable("Slottable");
function isSlottable(child) {
  return React3.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
}
function mergeProps(slotProps, childProps) {
  const overrideProps = { ...childProps };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue) {
        overrideProps[propName] = (...args) => {
          const result = childPropValue(...args);
          slotPropValue(...args);
          return result;
        };
      } else if (slotPropValue) {
        overrideProps[propName] = slotPropValue;
      }
    } else if (propName === "style") {
      overrideProps[propName] = { ...slotPropValue, ...childPropValue };
    } else if (propName === "className") {
      overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
  }
  return { ...slotProps, ...overrideProps };
}
function getElementRef(element) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.props.ref;
  }
  return element.props.ref || element.ref;
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-primitive/dist/index.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var NODES = [
  "a",
  "button",
  "div",
  "form",
  "h2",
  "h3",
  "img",
  "input",
  "label",
  "li",
  "nav",
  "ol",
  "p",
  "select",
  "span",
  "svg",
  "ul"
];
var Primitive = NODES.reduce((primitive, node) => {
  const Slot2 = createSlot(`Primitive.${node}`);
  const Node2 = React4.forwardRef((props, forwardedRef) => {
    const { asChild, ...primitiveProps } = props;
    const Comp = asChild ? Slot2 : node;
    if (typeof window !== "undefined") {
      window[Symbol.for("radix-ui")] = true;
    }
    return (0, import_jsx_runtime3.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
  });
  Node2.displayName = `Primitive.${node}`;
  return { ...primitive, [node]: Node2 };
}, {});

// node_modules/@strapi/upload/node_modules/@radix-ui/react-roving-focus/dist/index.mjs
var React13 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/primitive/dist/index.mjs
var canUseDOM = !!(typeof window !== "undefined" && window.document && window.document.createElement);
function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
  return function handleEvent(event) {
    originalEventHandler == null ? void 0 : originalEventHandler(event);
    if (checkForDefaultPrevented === false || !event.defaultPrevented) {
      return ourEventHandler == null ? void 0 : ourEventHandler(event);
    }
  };
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-collection/dist/index.mjs
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
function createCollection(name) {
  const PROVIDER_NAME = name + "CollectionProvider";
  const [createCollectionContext, createCollectionScope2] = createContextScope(PROVIDER_NAME);
  const [CollectionProviderImpl, useCollectionContext] = createCollectionContext(
    PROVIDER_NAME,
    { collectionRef: { current: null }, itemMap: /* @__PURE__ */ new Map() }
  );
  const CollectionProvider = (props) => {
    const { scope, children } = props;
    const ref = import_react.default.useRef(null);
    const itemMap = import_react.default.useRef(/* @__PURE__ */ new Map()).current;
    return (0, import_jsx_runtime4.jsx)(CollectionProviderImpl, { scope, itemMap, collectionRef: ref, children });
  };
  CollectionProvider.displayName = PROVIDER_NAME;
  const COLLECTION_SLOT_NAME = name + "CollectionSlot";
  const CollectionSlotImpl = createSlot(COLLECTION_SLOT_NAME);
  const CollectionSlot = import_react.default.forwardRef(
    (props, forwardedRef) => {
      const { scope, children } = props;
      const context = useCollectionContext(COLLECTION_SLOT_NAME, scope);
      const composedRefs = useComposedRefs(forwardedRef, context.collectionRef);
      return (0, import_jsx_runtime4.jsx)(CollectionSlotImpl, { ref: composedRefs, children });
    }
  );
  CollectionSlot.displayName = COLLECTION_SLOT_NAME;
  const ITEM_SLOT_NAME = name + "CollectionItemSlot";
  const ITEM_DATA_ATTR = "data-radix-collection-item";
  const CollectionItemSlotImpl = createSlot(ITEM_SLOT_NAME);
  const CollectionItemSlot = import_react.default.forwardRef(
    (props, forwardedRef) => {
      const { scope, children, ...itemData } = props;
      const ref = import_react.default.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const context = useCollectionContext(ITEM_SLOT_NAME, scope);
      import_react.default.useEffect(() => {
        context.itemMap.set(ref, { ref, ...itemData });
        return () => void context.itemMap.delete(ref);
      });
      return (0, import_jsx_runtime4.jsx)(CollectionItemSlotImpl, { ...{ [ITEM_DATA_ATTR]: "" }, ref: composedRefs, children });
    }
  );
  CollectionItemSlot.displayName = ITEM_SLOT_NAME;
  function useCollection2(scope) {
    const context = useCollectionContext(name + "CollectionConsumer", scope);
    const getItems = import_react.default.useCallback(() => {
      const collectionNode = context.collectionRef.current;
      if (!collectionNode) return [];
      const orderedNodes = Array.from(collectionNode.querySelectorAll(`[${ITEM_DATA_ATTR}]`));
      const items = Array.from(context.itemMap.values());
      const orderedItems = items.sort(
        (a, b) => orderedNodes.indexOf(a.ref.current) - orderedNodes.indexOf(b.ref.current)
      );
      return orderedItems;
    }, [context.collectionRef, context.itemMap]);
    return getItems;
  }
  return [
    { Provider: CollectionProvider, Slot: CollectionSlot, ItemSlot: CollectionItemSlot },
    useCollection2,
    createCollectionScope2
  ];
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-id/dist/index.mjs
var React7 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
var React6 = __toESM(require_react(), 1);
var useLayoutEffect2 = (globalThis == null ? void 0 : globalThis.document) ? React6.useLayoutEffect : () => {
};

// node_modules/@strapi/upload/node_modules/@radix-ui/react-id/dist/index.mjs
var useReactId = React7[" useId ".trim().toString()] || (() => void 0);
var count = 0;
function useId(deterministicId) {
  const [id, setId] = React7.useState(useReactId());
  useLayoutEffect2(() => {
    if (!deterministicId) setId((reactId) => reactId ?? String(count++));
  }, [deterministicId]);
  return deterministicId || (id ? `radix-${id}` : "");
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-callback-ref/dist/index.mjs
var React8 = __toESM(require_react(), 1);
function useCallbackRef(callback) {
  const callbackRef = React8.useRef(callback);
  React8.useEffect(() => {
    callbackRef.current = callback;
  });
  return React8.useMemo(() => (...args) => {
    var _a;
    return (_a = callbackRef.current) == null ? void 0 : _a.call(callbackRef, ...args);
  }, []);
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
var React11 = __toESM(require_react(), 1);
var React23 = __toESM(require_react(), 1);

// node_modules/@radix-ui/react-use-effect-event/node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
var React9 = __toESM(require_react(), 1);
var useLayoutEffect22 = (globalThis == null ? void 0 : globalThis.document) ? React9.useLayoutEffect : () => {
};

// node_modules/@radix-ui/react-use-effect-event/dist/index.mjs
var React10 = __toESM(require_react(), 1);
var useReactEffectEvent = React10[" useEffectEvent ".trim().toString()];
var useReactInsertionEffect = React10[" useInsertionEffect ".trim().toString()];

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
var useInsertionEffect = React11[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
function useControllableState({
  prop,
  defaultProp,
  onChange = () => {
  },
  caller
}) {
  const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
    defaultProp,
    onChange
  });
  const isControlled = prop !== void 0;
  const value = isControlled ? prop : uncontrolledProp;
  if (true) {
    const isControlledRef = React11.useRef(prop !== void 0);
    React11.useEffect(() => {
      const wasControlled = isControlledRef.current;
      if (wasControlled !== isControlled) {
        const from = wasControlled ? "controlled" : "uncontrolled";
        const to = isControlled ? "controlled" : "uncontrolled";
        console.warn(
          `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
        );
      }
      isControlledRef.current = isControlled;
    }, [isControlled, caller]);
  }
  const setValue = React11.useCallback(
    (nextValue) => {
      var _a;
      if (isControlled) {
        const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
        if (value2 !== prop) {
          (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value2);
        }
      } else {
        setUncontrolledProp(nextValue);
      }
    },
    [isControlled, prop, setUncontrolledProp, onChangeRef]
  );
  return [value, setValue];
}
function useUncontrolledState({
  defaultProp,
  onChange
}) {
  const [value, setValue] = React11.useState(defaultProp);
  const prevValueRef = React11.useRef(value);
  const onChangeRef = React11.useRef(onChange);
  useInsertionEffect(() => {
    onChangeRef.current = onChange;
  }, [onChange]);
  React11.useEffect(() => {
    var _a;
    if (prevValueRef.current !== value) {
      (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value);
      prevValueRef.current = value;
    }
  }, [value, prevValueRef]);
  return [value, setValue, onChangeRef];
}
function isFunction(value) {
  return typeof value === "function";
}
var SYNC_STATE = Symbol("RADIX:SYNC_STATE");

// node_modules/@strapi/upload/node_modules/@radix-ui/react-direction/dist/index.mjs
var React12 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var DirectionContext = React12.createContext(void 0);
function useDirection(localDir) {
  const globalDir = React12.useContext(DirectionContext);
  return localDir || globalDir || "ltr";
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-roving-focus/dist/index.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var ENTRY_FOCUS = "rovingFocusGroup.onEntryFocus";
var EVENT_OPTIONS = { bubbles: false, cancelable: true };
var GROUP_NAME = "RovingFocusGroup";
var [Collection, useCollection, createCollectionScope] = createCollection(GROUP_NAME);
var [createRovingFocusGroupContext, createRovingFocusGroupScope] = createContextScope(
  GROUP_NAME,
  [createCollectionScope]
);
var [RovingFocusProvider, useRovingFocusContext] = createRovingFocusGroupContext(GROUP_NAME);
var RovingFocusGroup = React13.forwardRef(
  (props, forwardedRef) => {
    return (0, import_jsx_runtime7.jsx)(Collection.Provider, { scope: props.__scopeRovingFocusGroup, children: (0, import_jsx_runtime7.jsx)(Collection.Slot, { scope: props.__scopeRovingFocusGroup, children: (0, import_jsx_runtime7.jsx)(RovingFocusGroupImpl, { ...props, ref: forwardedRef }) }) });
  }
);
RovingFocusGroup.displayName = GROUP_NAME;
var RovingFocusGroupImpl = React13.forwardRef((props, forwardedRef) => {
  const {
    __scopeRovingFocusGroup,
    orientation,
    loop = false,
    dir,
    currentTabStopId: currentTabStopIdProp,
    defaultCurrentTabStopId,
    onCurrentTabStopIdChange,
    onEntryFocus,
    preventScrollOnEntryFocus = false,
    ...groupProps
  } = props;
  const ref = React13.useRef(null);
  const composedRefs = useComposedRefs(forwardedRef, ref);
  const direction = useDirection(dir);
  const [currentTabStopId, setCurrentTabStopId] = useControllableState({
    prop: currentTabStopIdProp,
    defaultProp: defaultCurrentTabStopId ?? null,
    onChange: onCurrentTabStopIdChange,
    caller: GROUP_NAME
  });
  const [isTabbingBackOut, setIsTabbingBackOut] = React13.useState(false);
  const handleEntryFocus = useCallbackRef(onEntryFocus);
  const getItems = useCollection(__scopeRovingFocusGroup);
  const isClickFocusRef = React13.useRef(false);
  const [focusableItemsCount, setFocusableItemsCount] = React13.useState(0);
  React13.useEffect(() => {
    const node = ref.current;
    if (node) {
      node.addEventListener(ENTRY_FOCUS, handleEntryFocus);
      return () => node.removeEventListener(ENTRY_FOCUS, handleEntryFocus);
    }
  }, [handleEntryFocus]);
  return (0, import_jsx_runtime7.jsx)(
    RovingFocusProvider,
    {
      scope: __scopeRovingFocusGroup,
      orientation,
      dir: direction,
      loop,
      currentTabStopId,
      onItemFocus: React13.useCallback(
        (tabStopId) => setCurrentTabStopId(tabStopId),
        [setCurrentTabStopId]
      ),
      onItemShiftTab: React13.useCallback(() => setIsTabbingBackOut(true), []),
      onFocusableItemAdd: React13.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount + 1),
        []
      ),
      onFocusableItemRemove: React13.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount - 1),
        []
      ),
      children: (0, import_jsx_runtime7.jsx)(
        Primitive.div,
        {
          tabIndex: isTabbingBackOut || focusableItemsCount === 0 ? -1 : 0,
          "data-orientation": orientation,
          ...groupProps,
          ref: composedRefs,
          style: { outline: "none", ...props.style },
          onMouseDown: composeEventHandlers(props.onMouseDown, () => {
            isClickFocusRef.current = true;
          }),
          onFocus: composeEventHandlers(props.onFocus, (event) => {
            const isKeyboardFocus = !isClickFocusRef.current;
            if (event.target === event.currentTarget && isKeyboardFocus && !isTabbingBackOut) {
              const entryFocusEvent = new CustomEvent(ENTRY_FOCUS, EVENT_OPTIONS);
              event.currentTarget.dispatchEvent(entryFocusEvent);
              if (!entryFocusEvent.defaultPrevented) {
                const items = getItems().filter((item) => item.focusable);
                const activeItem = items.find((item) => item.active);
                const currentItem = items.find((item) => item.id === currentTabStopId);
                const candidateItems = [activeItem, currentItem, ...items].filter(
                  Boolean
                );
                const candidateNodes = candidateItems.map((item) => item.ref.current);
                focusFirst(candidateNodes, preventScrollOnEntryFocus);
              }
            }
            isClickFocusRef.current = false;
          }),
          onBlur: composeEventHandlers(props.onBlur, () => setIsTabbingBackOut(false))
        }
      )
    }
  );
});
var ITEM_NAME = "RovingFocusGroupItem";
var RovingFocusGroupItem = React13.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeRovingFocusGroup,
      focusable = true,
      active = false,
      tabStopId,
      children,
      ...itemProps
    } = props;
    const autoId = useId();
    const id = tabStopId || autoId;
    const context = useRovingFocusContext(ITEM_NAME, __scopeRovingFocusGroup);
    const isCurrentTabStop = context.currentTabStopId === id;
    const getItems = useCollection(__scopeRovingFocusGroup);
    const { onFocusableItemAdd, onFocusableItemRemove, currentTabStopId } = context;
    React13.useEffect(() => {
      if (focusable) {
        onFocusableItemAdd();
        return () => onFocusableItemRemove();
      }
    }, [focusable, onFocusableItemAdd, onFocusableItemRemove]);
    return (0, import_jsx_runtime7.jsx)(
      Collection.ItemSlot,
      {
        scope: __scopeRovingFocusGroup,
        id,
        focusable,
        active,
        children: (0, import_jsx_runtime7.jsx)(
          Primitive.span,
          {
            tabIndex: isCurrentTabStop ? 0 : -1,
            "data-orientation": context.orientation,
            ...itemProps,
            ref: forwardedRef,
            onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
              if (!focusable) event.preventDefault();
              else context.onItemFocus(id);
            }),
            onFocus: composeEventHandlers(props.onFocus, () => context.onItemFocus(id)),
            onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
              if (event.key === "Tab" && event.shiftKey) {
                context.onItemShiftTab();
                return;
              }
              if (event.target !== event.currentTarget) return;
              const focusIntent = getFocusIntent(event, context.orientation, context.dir);
              if (focusIntent !== void 0) {
                if (event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) return;
                event.preventDefault();
                const items = getItems().filter((item) => item.focusable);
                let candidateNodes = items.map((item) => item.ref.current);
                if (focusIntent === "last") candidateNodes.reverse();
                else if (focusIntent === "prev" || focusIntent === "next") {
                  if (focusIntent === "prev") candidateNodes.reverse();
                  const currentIndex = candidateNodes.indexOf(event.currentTarget);
                  candidateNodes = context.loop ? wrapArray(candidateNodes, currentIndex + 1) : candidateNodes.slice(currentIndex + 1);
                }
                setTimeout(() => focusFirst(candidateNodes));
              }
            }),
            children: typeof children === "function" ? children({ isCurrentTabStop, hasTabStop: currentTabStopId != null }) : children
          }
        )
      }
    );
  }
);
RovingFocusGroupItem.displayName = ITEM_NAME;
var MAP_KEY_TO_FOCUS_INTENT = {
  ArrowLeft: "prev",
  ArrowUp: "prev",
  ArrowRight: "next",
  ArrowDown: "next",
  PageUp: "first",
  Home: "first",
  PageDown: "last",
  End: "last"
};
function getDirectionAwareKey(key, dir) {
  if (dir !== "rtl") return key;
  return key === "ArrowLeft" ? "ArrowRight" : key === "ArrowRight" ? "ArrowLeft" : key;
}
function getFocusIntent(event, orientation, dir) {
  const key = getDirectionAwareKey(event.key, dir);
  if (orientation === "vertical" && ["ArrowLeft", "ArrowRight"].includes(key)) return void 0;
  if (orientation === "horizontal" && ["ArrowUp", "ArrowDown"].includes(key)) return void 0;
  return MAP_KEY_TO_FOCUS_INTENT[key];
}
function focusFirst(candidates, preventScroll = false) {
  const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
  for (const candidate of candidates) {
    if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
    candidate.focus({ preventScroll });
    if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
  }
}
function wrapArray(array, startIndex) {
  return array.map((_, index) => array[(startIndex + index) % array.length]);
}
var Root = RovingFocusGroup;
var Item = RovingFocusGroupItem;

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle/dist/index.mjs
var React14 = __toESM(require_react(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var NAME = "Toggle";
var Toggle = React14.forwardRef((props, forwardedRef) => {
  const { pressed: pressedProp, defaultPressed, onPressedChange, ...buttonProps } = props;
  const [pressed, setPressed] = useControllableState({
    prop: pressedProp,
    onChange: onPressedChange,
    defaultProp: defaultPressed ?? false,
    caller: NAME
  });
  return (0, import_jsx_runtime8.jsx)(
    Primitive.button,
    {
      type: "button",
      "aria-pressed": pressed,
      "data-state": pressed ? "on" : "off",
      "data-disabled": props.disabled ? "" : void 0,
      ...buttonProps,
      ref: forwardedRef,
      onClick: composeEventHandlers(props.onClick, () => {
        if (!props.disabled) {
          setPressed(!pressed);
        }
      })
    }
  );
});
Toggle.displayName = NAME;

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle-group/dist/index.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var TOGGLE_GROUP_NAME = "ToggleGroup";
var [createToggleGroupContext, createToggleGroupScope] = createContextScope(TOGGLE_GROUP_NAME, [
  createRovingFocusGroupScope
]);
var useRovingFocusGroupScope = createRovingFocusGroupScope();
var ToggleGroup = import_react3.default.forwardRef((props, forwardedRef) => {
  const { type, ...toggleGroupProps } = props;
  if (type === "single") {
    const singleProps = toggleGroupProps;
    return (0, import_jsx_runtime9.jsx)(ToggleGroupImplSingle, { ...singleProps, ref: forwardedRef });
  }
  if (type === "multiple") {
    const multipleProps = toggleGroupProps;
    return (0, import_jsx_runtime9.jsx)(ToggleGroupImplMultiple, { ...multipleProps, ref: forwardedRef });
  }
  throw new Error(`Missing prop \`type\` expected on \`${TOGGLE_GROUP_NAME}\``);
});
ToggleGroup.displayName = TOGGLE_GROUP_NAME;
var [ToggleGroupValueProvider, useToggleGroupValueContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
var ToggleGroupImplSingle = import_react3.default.forwardRef((props, forwardedRef) => {
  const {
    value: valueProp,
    defaultValue,
    onValueChange = () => {
    },
    ...toggleGroupSingleProps
  } = props;
  const [value, setValue] = useControllableState({
    prop: valueProp,
    defaultProp: defaultValue ?? "",
    onChange: onValueChange,
    caller: TOGGLE_GROUP_NAME
  });
  return (0, import_jsx_runtime9.jsx)(
    ToggleGroupValueProvider,
    {
      scope: props.__scopeToggleGroup,
      type: "single",
      value: import_react3.default.useMemo(() => value ? [value] : [], [value]),
      onItemActivate: setValue,
      onItemDeactivate: import_react3.default.useCallback(() => setValue(""), [setValue]),
      children: (0, import_jsx_runtime9.jsx)(ToggleGroupImpl, { ...toggleGroupSingleProps, ref: forwardedRef })
    }
  );
});
var ToggleGroupImplMultiple = import_react3.default.forwardRef((props, forwardedRef) => {
  const {
    value: valueProp,
    defaultValue,
    onValueChange = () => {
    },
    ...toggleGroupMultipleProps
  } = props;
  const [value, setValue] = useControllableState({
    prop: valueProp,
    defaultProp: defaultValue ?? [],
    onChange: onValueChange,
    caller: TOGGLE_GROUP_NAME
  });
  const handleButtonActivate = import_react3.default.useCallback(
    (itemValue) => setValue((prevValue = []) => [...prevValue, itemValue]),
    [setValue]
  );
  const handleButtonDeactivate = import_react3.default.useCallback(
    (itemValue) => setValue((prevValue = []) => prevValue.filter((value2) => value2 !== itemValue)),
    [setValue]
  );
  return (0, import_jsx_runtime9.jsx)(
    ToggleGroupValueProvider,
    {
      scope: props.__scopeToggleGroup,
      type: "multiple",
      value,
      onItemActivate: handleButtonActivate,
      onItemDeactivate: handleButtonDeactivate,
      children: (0, import_jsx_runtime9.jsx)(ToggleGroupImpl, { ...toggleGroupMultipleProps, ref: forwardedRef })
    }
  );
});
ToggleGroup.displayName = TOGGLE_GROUP_NAME;
var [ToggleGroupContext, useToggleGroupContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
var ToggleGroupImpl = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeToggleGroup,
      disabled = false,
      rovingFocus = true,
      orientation,
      dir,
      loop = true,
      ...toggleGroupProps
    } = props;
    const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeToggleGroup);
    const direction = useDirection(dir);
    const commonProps = { role: "group", dir: direction, ...toggleGroupProps };
    return (0, import_jsx_runtime9.jsx)(ToggleGroupContext, { scope: __scopeToggleGroup, rovingFocus, disabled, children: rovingFocus ? (0, import_jsx_runtime9.jsx)(
      Root,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        orientation,
        dir: direction,
        loop,
        children: (0, import_jsx_runtime9.jsx)(Primitive.div, { ...commonProps, ref: forwardedRef })
      }
    ) : (0, import_jsx_runtime9.jsx)(Primitive.div, { ...commonProps, ref: forwardedRef }) });
  }
);
var ITEM_NAME2 = "ToggleGroupItem";
var ToggleGroupItem = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const valueContext = useToggleGroupValueContext(ITEM_NAME2, props.__scopeToggleGroup);
    const context = useToggleGroupContext(ITEM_NAME2, props.__scopeToggleGroup);
    const rovingFocusGroupScope = useRovingFocusGroupScope(props.__scopeToggleGroup);
    const pressed = valueContext.value.includes(props.value);
    const disabled = context.disabled || props.disabled;
    const commonProps = { ...props, pressed, disabled };
    const ref = import_react3.default.useRef(null);
    return context.rovingFocus ? (0, import_jsx_runtime9.jsx)(
      Item,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        focusable: !disabled,
        active: pressed,
        ref,
        children: (0, import_jsx_runtime9.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef })
      }
    ) : (0, import_jsx_runtime9.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef });
  }
);
ToggleGroupItem.displayName = ITEM_NAME2;
var ToggleGroupItemImpl = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const { __scopeToggleGroup, value, ...itemProps } = props;
    const valueContext = useToggleGroupValueContext(ITEM_NAME2, __scopeToggleGroup);
    const singleProps = { role: "radio", "aria-checked": props.pressed, "aria-pressed": void 0 };
    const typeProps = valueContext.type === "single" ? singleProps : void 0;
    return (0, import_jsx_runtime9.jsx)(
      Toggle,
      {
        ...typeProps,
        ...itemProps,
        ref: forwardedRef,
        onPressedChange: (pressed) => {
          if (pressed) {
            valueContext.onItemActivate(value);
          } else {
            valueContext.onItemDeactivate(value);
          }
        }
      }
    );
  }
);
var Root2 = ToggleGroup;
var Item2 = ToggleGroupItem;

// node_modules/@strapi/upload/dist/admin/future/services/folders.mjs
var isDataEnvelope = (response) => typeof response === "object" && response !== null && "data" in response;
var unwrapData = (response) => isDataEnvelope(response) ? response.data : response;
var foldersApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getFolders: builder.query({
      query: (params = {}) => {
        const { parentId } = params;
        const queryParams = {
          // Match sidebar FolderTree order (server getStructure uses sortBy('name')).
          sort: "name:ASC"
        };
        if (parentId != null) {
          queryParams["filters"] = {
            $and: [
              {
                parent: {
                  id: parentId
                }
              }
            ]
          };
        } else {
          queryParams["filters"] = {
            $and: [
              {
                parent: {
                  id: {
                    $null: true
                  }
                }
              }
            ]
          };
        }
        return {
          url: "/upload/folders",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => unwrapData(response),
      providesTags: (results) => {
        if (results) {
          return [
            ...results.map(({ id }) => ({
              type: "Folder",
              id
            })),
            {
              type: "Folder",
              id: "LIST"
            }
          ];
        }
        return [
          {
            type: "Folder",
            id: "LIST"
          }
        ];
      }
    }),
    createFolder: builder.mutation({
      query: (body) => ({
        url: "/upload/folders",
        method: "POST",
        data: body
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: [
        {
          type: "Folder",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    }),
    /**
    * Hierarchical folder tree used by the FolderTree sidebar. Returned as a
    * single nested array — the server walks the folders table once and
    * assembles parent/child relationships.
    *
    * TODO: filter results like `admin-folder.find` for folder-scoped roles
    * (v1 returns all folders; relies on global `plugin::upload.read`).
    */
    getFolderStructure: builder.query({
      query: () => ({
        url: "/upload/folder-structure",
        method: "GET"
      }),
      // TODO: align GetFolderStructure contract with the real /upload/folder-structure
      // envelope and drop this defensive unwrap (data[] vs { data: [] }).
      transformResponse: (response) => (response == null ? void 0 : response.data) ?? response ?? [],
      providesTags: [
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    }),
    /**
    * Flat list of every folder, used to populate the "Location" select in the
    * asset details drawer. No parent filter — we want the entire tree in one
    * query so the select can render any destination folder.
    */
    getAllFolders: builder.query({
      query: () => ({
        url: "/upload/folders",
        method: "GET"
      }),
      transformResponse: (response) => unwrapData(response ?? []),
      providesTags: (results) => {
        if (results) {
          return [
            ...results.map(({ id }) => ({
              type: "Folder",
              id
            })),
            {
              type: "Folder",
              id: "LIST"
            }
          ];
        }
        return [
          {
            type: "Folder",
            id: "LIST"
          }
        ];
      }
    }),
    getFolder: builder.query({
      query: ({ id }) => ({
        url: `/upload/folders/${id}`,
        method: "GET",
        config: {
          params: {
            populate: {
              parent: {
                populate: {
                  parent: "*"
                }
              },
              children: {
                count: true
              },
              files: {
                count: true
              }
            }
          }
        }
      }),
      transformResponse: (response) => response.data,
      providesTags: (_result, _error, { id }) => [
        {
          type: "Folder",
          id
        }
      ]
    }),
    bulkMove: builder.mutation({
      query: ({ fileIds = [], folderIds = [], destinationFolderId }) => ({
        url: "/upload/actions/bulk-move",
        method: "POST",
        data: {
          fileIds,
          folderIds,
          destinationFolderId
        }
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: [
        {
          type: "Asset",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    })
  })
});
var { useCreateFolderMutation, useGetFoldersQuery, useGetFolderQuery, useGetAllFoldersQuery, useGetFolderStructureQuery, useBulkMoveMutation } = foldersApi;

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var React19 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/services/assets.mjs
var assetsApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getAssets: builder.query({
      query: (params = {}) => {
        const { folder, ...rest } = params;
        const queryParams = {
          ...rest
        };
        if (folder != null) {
          queryParams["filters"] = {
            $and: [
              {
                folder: {
                  id: folder
                }
              }
            ]
          };
        } else {
          queryParams["filters"] = {
            $and: [
              {
                folder: {
                  id: {
                    $null: true
                  }
                }
              }
            ]
          };
        }
        return {
          url: "/upload/files",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => response,
      providesTags: (result) => result ? [
        ...result.results.map(({ id }) => ({
          type: "Asset",
          id
        })),
        {
          type: "Asset",
          id: "LIST"
        }
      ] : [
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    getAsset: builder.query({
      query: (id) => ({
        url: `/upload/files/${id}`,
        method: "GET"
      }),
      providesTags: (_result, _error, id) => [
        {
          type: "Asset",
          id
        }
      ]
    }),
    /**
    * Update the editable metadata of an existing asset.
    * Hits the legacy `POST /upload?id=<id>` endpoint which dispatches to
    * `admin-upload.updateFileInfo`.
    */
    updateAsset: builder.mutation({
      query: ({ id, fileInfo }) => {
        const formData = new FormData();
        formData.append("fileInfo", JSON.stringify(fileInfo));
        return {
          url: "/upload",
          method: "POST",
          data: formData,
          config: {
            params: {
              id
            }
          }
        };
      },
      invalidatesTags: (_result, _error, { id }) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    /**
    * Replace the binary content of an existing asset.
    * Hits `POST /upload?id=<id>` with a multipart body — the controller
    * dispatches to `admin-upload.replaceFile` when a `files` part is present.
    * Uses the standard axios baseQuery (no streaming) since we only ever
    * replace one file at a time and don't need per-byte progress here.
    */
    replaceAsset: builder.mutation({
      query: ({ id, file, fileInfo }) => {
        const formData = new FormData();
        formData.append("files", file);
        if (fileInfo) {
          formData.append("fileInfo", JSON.stringify(fileInfo));
        }
        return {
          url: "/upload",
          method: "POST",
          data: formData,
          config: {
            params: {
              id
            }
          }
        };
      },
      invalidatesTags: (_result, _error, { id }) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    /**
    * Permanently delete an asset by id. Hits the same endpoint as the legacy
    * `useRemoveAsset` hook so server behaviour is unchanged.
    */
    deleteAsset: builder.mutation({
      query: (id) => ({
        url: `/upload/files/${id}`,
        method: "DELETE"
      }),
      invalidatesTags: (_result, _error, id) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    })
  })
});
var { useGetAssetsQuery, useGetAssetQuery, useUpdateAssetMutation, useReplaceAssetMutation, useDeleteAssetMutation } = assetsApi;

// node_modules/@strapi/upload/dist/admin/future/services/settings.mjs
var settingsApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getSettings: builder.query({
      query: () => ({
        url: "/upload/settings",
        method: "GET"
      })
    })
  })
});
var { useGetSettingsQuery } = settingsApi;

// node_modules/@strapi/upload/dist/admin/future/utils/downloadFile.mjs
var downloadFile = async (url, fileName) => {
  const response = await fetch(url);
  const blob = await response.blob();
  const objectUrl = window.URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = objectUrl;
  anchor.setAttribute("download", fileName);
  anchor.click();
  window.URL.revokeObjectURL(objectUrl);
};

// node_modules/@strapi/upload/dist/admin/future/utils/getAssetIcon.mjs
var DOC_ICON_MAP = {
  pdf: j5,
  csv: Q5,
  xls: K5,
  zip: X5
};
var getAssetIcon = (mime, ext) => {
  const fileExtension = getFileExtension(ext);
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    return r3;
  }
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Video)) {
    return wn;
  }
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Audio)) {
    return g3;
  }
  return fileExtension ? DOC_ICON_MAP[fileExtension] || Z5 : Z5;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderInfo.mjs
var useFolderInfo = (currentFolderId) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { data: currentFolder, isLoading } = useGetFolderQuery({
    id: currentFolderId
  }, {
    skip: currentFolderId === null
  });
  const { data: rootAssetsData, isLoading: isRootAssetsLoading } = useGetAssetsQuery({
    folder: null,
    pageSize: 1
  }, {
    skip: currentFolderId !== null
  });
  const homeLabel = formatMessage({
    id: getTranslationKey("plugin.home"),
    defaultMessage: "Home"
  });
  if (currentFolderId === null) {
    if (isRootAssetsLoading) {
      return {
        title: homeLabel,
        itemCount: 0
      };
    }
    return {
      title: homeLabel,
      itemCount: ((_a = rootAssetsData == null ? void 0 : rootAssetsData.pagination) == null ? void 0 : _a.total) ?? 0
    };
  }
  if (isLoading || !currentFolder) {
    return {
      title: "",
      itemCount: 0
    };
  }
  return {
    title: currentFolder.name,
    itemCount: ((_b = currentFolder.files) == null ? void 0 : _b.count) ?? 0
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetCropEditor.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var React17 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/useCropImg.mjs
var React16 = __toESM(require_react(), 1);
var QUALITY = 1;
var resolveCornerResize = ({ anchorX, anchorY, point, aspectRatio }) => {
  let width = Math.abs(point.x - anchorX);
  let height = Math.abs(point.y - anchorY);
  if (aspectRatio) {
    if (width / aspectRatio >= height) {
      height = width / aspectRatio;
    } else {
      width = height * aspectRatio;
    }
  }
  const x = point.x < anchorX ? anchorX - width : anchorX;
  const y = point.y < anchorY ? anchorY - height : anchorY;
  return {
    x,
    y,
    width,
    height
  };
};
var useCropImg = () => {
  const [naturalSize, setNaturalSize] = React16.useState({
    width: 0,
    height: 0
  });
  const [crop, setCrop] = React16.useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0
  });
  const [aspectRatio, setAspectRatioState] = React16.useState(null);
  const imageRef = React16.useRef(null);
  const init = React16.useCallback((image) => {
    imageRef.current = image;
    const next = {
      width: image.naturalWidth,
      height: image.naturalHeight
    };
    setNaturalSize(next);
    setCrop({
      x: 0,
      y: 0,
      width: next.width,
      height: next.height
    });
  }, []);
  const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
  const setCropSize = React16.useCallback((next) => {
    setCrop((prev) => {
      const maxW = naturalSize.width - prev.x;
      const maxH = naturalSize.height - prev.y;
      let width = next.width !== void 0 ? clamp(next.width, 1, maxW) : prev.width;
      let height = next.height !== void 0 ? clamp(next.height, 1, maxH) : prev.height;
      if (aspectRatio) {
        if (next.width !== void 0) {
          height = clamp(width / aspectRatio, 1, maxH);
        } else if (next.height !== void 0) {
          width = clamp(height * aspectRatio, 1, maxW);
        }
      }
      return {
        ...prev,
        width,
        height
      };
    });
  }, [
    naturalSize.width,
    naturalSize.height,
    aspectRatio
  ]);
  const setCropPosition = React16.useCallback((next) => {
    setCrop((prev) => {
      const x = next.x !== void 0 ? clamp(next.x, 0, naturalSize.width - prev.width) : prev.x;
      const y = next.y !== void 0 ? clamp(next.y, 0, naturalSize.height - prev.height) : prev.y;
      return {
        ...prev,
        x,
        y
      };
    });
  }, [
    naturalSize.width,
    naturalSize.height
  ]);
  const setAspectRatio = React16.useCallback((ratio) => {
    setAspectRatioState(ratio);
    if (ratio) {
      setCrop((prev) => {
        const maxW = naturalSize.width - prev.x;
        const maxH = naturalSize.height - prev.y;
        let width = prev.width;
        let height = width / ratio;
        if (height > maxH) {
          height = maxH;
          width = height * ratio;
        }
        if (width > maxW) {
          width = maxW;
          height = width / ratio;
        }
        return {
          ...prev,
          width: Math.round(width),
          height: Math.round(height)
        };
      });
    }
  }, [
    naturalSize.width,
    naturalSize.height
  ]);
  const produceFile = React16.useCallback((name, mimeType, lastModifiedDate) => new Promise((resolve, reject) => {
    const image = imageRef.current;
    if (!image) {
      reject(new Error("Image not ready: call init() before produceFile()."));
      return;
    }
    const canvas = document.createElement("canvas");
    canvas.width = Math.max(1, Math.round(crop.width));
    canvas.height = Math.max(1, Math.round(crop.height));
    const context = canvas.getContext("2d");
    if (!context) {
      reject(new Error("Could not get a 2D canvas context to crop the image."));
      return;
    }
    context.drawImage(image, crop.x, crop.y, crop.width, crop.height, 0, 0, canvas.width, canvas.height);
    canvas.toBlob((blob) => {
      if (!blob) {
        reject(new Error("Could not export the cropped image to a blob."));
        return;
      }
      resolve(new File([
        blob
      ], name, {
        type: mimeType,
        lastModified: lastModifiedDate ? new Date(lastModifiedDate).getTime() : Date.now()
      }));
    }, mimeType, QUALITY);
  }), [
    crop.x,
    crop.y,
    crop.width,
    crop.height
  ]);
  return {
    init,
    crop,
    naturalSize,
    aspectRatio,
    setCropSize,
    setCropPosition,
    setAspectRatio,
    produceFile,
    // Round-aware getters for the px inputs.
    width: Math.round(crop.width),
    height: Math.round(crop.height)
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetCropEditor.mjs
var FOCAL_DIAMETER_REM = 5.6;
var HANDLE_PX = 12;
var Overlay = gt(v)`
  position: fixed;
  z-index: 1200;
  flex-direction: column;
  top: ${({ theme }) => theme.spaces[1]};
  left: ${({ theme }) => theme.spaces[1]};
  right: ${({ theme }) => theme.spaces[1]};
  bottom: ${({ theme }) => theme.spaces[1]};
  border-radius: ${({ theme }) => theme.borderRadius};
  border: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
`;
var HeaderBar = gt(v)`
  width: 100%;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[3]} ${theme.spaces[5]}`};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
`;
var Body = gt(S)`
  width: 100%;
  position: relative;
  flex: 1;
  min-height: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      ${({ theme }) => theme.colors.neutral0} 0% 50%
    )
    50% / 20px 20px;
`;
var CropArea = gt.div`
  position: relative;
  max-width: 100%;
  max-height: 100%;
  ${({ $aspect }) => $aspect ? `aspect-ratio: ${$aspect};` : ""}

  img {
    display: block;
    width: 100%;
    height: 100%;
    user-select: none;
    -webkit-user-drag: none;
  }
`;
var CropOverlay = gt.div`
  position: absolute;
  border: 1px dashed ${({ theme }) => theme.colors.primary600};
  box-shadow: 0 0 0 9999px rgba(33, 33, 52, 0.5);
  cursor: move;
`;
var ResizeHandle = gt.button`
  position: absolute;
  width: ${HANDLE_PX}px;
  height: ${HANDLE_PX}px;
  margin: -${HANDLE_PX / 2}px;
  padding: 0;
  border: 1px solid ${({ theme }) => theme.colors.primary600};
  border-radius: 2px;
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: ${({ $cursor }) => $cursor};
`;
var FocalPointHandle = gt.button`
  position: absolute;
  width: ${FOCAL_DIAMETER_REM}rem;
  height: ${FOCAL_DIAMETER_REM}rem;
  margin: ${-FOCAL_DIAMETER_REM / 2}rem 0 0 ${-FOCAL_DIAMETER_REM / 2}rem;
  border-radius: 50%;
  border: 1px solid ${({ theme }) => theme.colors.neutral800};
  background: transparent;
  cursor: grab;
  padding: 0;

  &::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.16);
    transform: translate(-50%, -50%);
  }
  &::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: ${({ theme }) => theme.colors.neutral800};
    transform: translate(-50%, -50%);
  }

  &:active {
    cursor: grabbing;
  }
`;
var InfoBox = gt(S)`
  position: absolute;
  right: ${({ theme }) => theme.spaces[1]};
  bottom: ${({ theme }) => theme.spaces[1]};
  width: 100%;
  max-width: 32rem;
  padding: ${({ theme }) => theme.spaces[3]};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colorScheme === "dark" ? theme.colors.neutral150 : theme.colors.neutral900};
  z-index: 20;
`;
var FooterBar = gt(v)`
  width: 100%;
  justify-content: space-between;
  padding: ${({ theme }) => `${theme.spaces[3]} ${theme.spaces[5]}`};
  border-top: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
`;
var FieldRow = gt(Dd.Root)`
  flex-direction: row;
  align-items: center;
`;
var FieldNumberInput = gt(Ud)`
  width: 8.4rem;
`;
var LabelIcon = gt(Dd.Label)`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 1.6rem;
  height: 1.6rem;
`;
var BackgroundPreserveRatioContainer = gt(S)`
  position: absolute;
  top: 50%;
  left: 0;
  transform: translateY(-50%);

  svg {
    display: block;
  }
`;
var BackgroundPreserveRatio = () => (0, import_jsx_runtime10.jsx)(BackgroundPreserveRatioContainer, {
  children: (0, import_jsx_runtime10.jsx)("svg", {
    width: "17",
    height: "49",
    viewBox: "0 0 17 49",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    children: (0, import_jsx_runtime10.jsx)("path", {
      d: "M0.5 0.5H8.5C12.9183 0.5 16.5 4.08172 16.5 8.5M0.5 48.5H8.5C12.9183 48.5 16.5 44.9183 16.5 40.5",
      stroke: "#666687",
      strokeLinecap: "round"
    })
  })
});
var AssetCropEditor = ({ asset, isBusy = false, onClose, onApply, onSaveAsCopy }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const theme = st();
  const isDark = theme.colorScheme === "dark";
  const infoTextColor = isDark ? "neutral1000" : "neutral0";
  const infoMutedColor = isDark ? "neutral600" : "neutral200";
  const imgRef = React17.useRef(null);
  const cropAreaRef = React17.useRef(null);
  const { init, crop, naturalSize, aspectRatio, setCropSize, setCropPosition, setAspectRatio, produceFile, width, height } = useCropImg();
  const [aspectLocked, setAspectLocked] = React17.useState(false);
  const [focal, setFocal] = React17.useState(asset.focalPoint ?? {
    x: 50,
    y: 50
  });
  const rawImageUrl = prefixFileUrlWithBackendUrl(asset.url);
  const cacheKey = asset.updatedAt ? new Date(asset.updatedAt).getTime() : void 0;
  const imageUrl = cacheKey !== void 0 ? `${rawImageUrl}${rawImageUrl.includes("?") ? "&" : "?"}v=${cacheKey}` : rawImageUrl;
  const handleImageLoad = () => {
    if (imgRef.current) {
      init(imgRef.current);
    }
  };
  const pointerToNatural = (event) => {
    var _a;
    const rect = (_a = cropAreaRef.current) == null ? void 0 : _a.getBoundingClientRect();
    if (!rect || !naturalSize.width || !naturalSize.height) return null;
    const ratioX = naturalSize.width / rect.width;
    const ratioY = naturalSize.height / rect.height;
    return {
      x: (event.clientX - rect.left) * ratioX,
      y: (event.clientY - rect.top) * ratioY
    };
  };
  const handleMovePointerDown = (event) => {
    event.preventDefault();
    event.stopPropagation();
    const start = pointerToNatural(event);
    if (!start) return;
    const startCrop = {
      ...crop
    };
    const move = (e) => {
      const next = pointerToNatural(e);
      if (!next) return;
      setCropPosition({
        x: startCrop.x + (next.x - start.x),
        y: startCrop.y + (next.y - start.y)
      });
    };
    const up = () => {
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", up);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const handleResizePointerDown = (corner) => (event) => {
    event.preventDefault();
    event.stopPropagation();
    const startCrop = {
      ...crop
    };
    const anchorX = corner === "tl" || corner === "bl" ? startCrop.x + startCrop.width : startCrop.x;
    const anchorY = corner === "tl" || corner === "tr" ? startCrop.y + startCrop.height : startCrop.y;
    const move = (e) => {
      const point = pointerToNatural(e);
      if (!point) return;
      const { x, y, width: w, height: h } = resolveCornerResize({
        anchorX,
        anchorY,
        point,
        aspectRatio: aspectLocked ? aspectRatio : null
      });
      setCropPosition({
        x,
        y
      });
      setCropSize({
        width: w,
        height: h
      });
    };
    const up = () => {
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", up);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const toggleAspectLock = () => {
    setAspectLocked((locked) => {
      const next = !locked;
      setAspectRatio(next && height ? width / height : null);
      return next;
    });
  };
  const handleFocalPointerDown = (event) => {
    event.preventDefault();
    event.stopPropagation();
    const move = (e) => {
      const point = pointerToNatural(e);
      if (!point) return;
      const px = (point.x - crop.x) / crop.width * 100;
      const py = (point.y - crop.y) / crop.height * 100;
      setFocal({
        x: Math.round(Math.min(100, Math.max(0, px))),
        y: Math.round(Math.min(100, Math.max(0, py)))
      });
    };
    const up = () => {
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", up);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const focalPxX = Math.round(focal.x / 100 * width);
  const focalPxY = Math.round(focal.y / 100 * height);
  const setFocalPx = (axis, value) => {
    const dim = axis === "x" ? width : height;
    if (!dim) return;
    const pct = Math.min(100, Math.max(0, value / dim * 100));
    setFocal((prev) => ({
      ...prev,
      [axis]: Math.round(pct)
    }));
  };
  const cropPercents = naturalSize.width && naturalSize.height ? {
    left: crop.x / naturalSize.width * 100,
    top: crop.y / naturalSize.height * 100,
    width: crop.width / naturalSize.width * 100,
    height: crop.height / naturalSize.height * 100
  } : null;
  const isReady = cropPercents !== null;
  const handleAction = async (action) => {
    if (!isReady) return;
    let file;
    try {
      file = await produceFile(asset.name, asset.mime ?? "image/png", asset.updatedAt);
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.crop.export-error"),
          defaultMessage: "Could not process the cropped image."
        })
      });
      return;
    }
    const roundedFocal = {
      x: Math.round(focal.x),
      y: Math.round(focal.y)
    };
    if (action === "apply") {
      onApply(file, roundedFocal);
    } else {
      onSaveAsCopy(file, roundedFocal);
    }
  };
  return (0, import_jsx_runtime10.jsx)(un, {
    children: (0, import_jsx_runtime10.jsx)(yu, {
      onEscape: onClose,
      children: (0, import_jsx_runtime10.jsxs)(Overlay, {
        children: [
          (0, import_jsx_runtime10.jsxs)(HeaderBar, {
            alignItems: "center",
            children: [
              (0, import_jsx_runtime10.jsx)(S5, {
                "aria-hidden": true
              }),
              (0, import_jsx_runtime10.jsx)(A, {
                variant: "omega",
                fontWeight: "bold",
                children: formatMessage({
                  id: getTranslationKey("asset-details.crop.title"),
                  defaultMessage: "Crop & Focus area"
                })
              })
            ]
          }),
          (0, import_jsx_runtime10.jsxs)(Body, {
            children: [
              (0, import_jsx_runtime10.jsxs)(CropArea, {
                ref: cropAreaRef,
                $aspect: naturalSize.width && naturalSize.height ? naturalSize.width / naturalSize.height : void 0,
                children: [
                  (0, import_jsx_runtime10.jsx)("img", {
                    ref: imgRef,
                    src: imageUrl,
                    alt: asset.name,
                    crossOrigin: "anonymous",
                    onLoad: handleImageLoad,
                    draggable: false
                  }),
                  cropPercents ? (0, import_jsx_runtime10.jsxs)(CropOverlay, {
                    style: {
                      left: `${cropPercents.left}%`,
                      top: `${cropPercents.top}%`,
                      width: `${cropPercents.width}%`,
                      height: `${cropPercents.height}%`
                    },
                    onPointerDown: handleMovePointerDown,
                    children: [
                      (0, import_jsx_runtime10.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.top-left"),
                          defaultMessage: "Resize top-left"
                        }),
                        $cursor: "nwse-resize",
                        style: {
                          left: 0,
                          top: 0
                        },
                        onPointerDown: handleResizePointerDown("tl")
                      }),
                      (0, import_jsx_runtime10.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.top-right"),
                          defaultMessage: "Resize top-right"
                        }),
                        $cursor: "nesw-resize",
                        style: {
                          right: 0,
                          top: 0
                        },
                        onPointerDown: handleResizePointerDown("tr")
                      }),
                      (0, import_jsx_runtime10.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.bottom-left"),
                          defaultMessage: "Resize bottom-left"
                        }),
                        $cursor: "nesw-resize",
                        style: {
                          left: 0,
                          bottom: 0
                        },
                        onPointerDown: handleResizePointerDown("bl")
                      }),
                      (0, import_jsx_runtime10.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.bottom-right"),
                          defaultMessage: "Resize bottom-right"
                        }),
                        $cursor: "nwse-resize",
                        style: {
                          right: 0,
                          bottom: 0
                        },
                        onPointerDown: handleResizePointerDown("br")
                      }),
                      (0, import_jsx_runtime10.jsx)(FocalPointHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.focal-point"),
                          defaultMessage: "Focal point"
                        }),
                        style: {
                          left: `${focal.x}%`,
                          top: `${focal.y}%`
                        },
                        onPointerDown: handleFocalPointerDown
                      })
                    ]
                  }) : null
                ]
              }),
              (0, import_jsx_runtime10.jsxs)(InfoBox, {
                children: [
                  (0, import_jsx_runtime10.jsxs)(v, {
                    direction: "column",
                    alignItems: "stretch",
                    gap: 1,
                    paddingBottom: 3,
                    children: [
                      (0, import_jsx_runtime10.jsx)(A, {
                        variant: "omega",
                        fontWeight: "bold",
                        textColor: infoTextColor,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.crop.title"),
                          defaultMessage: "Crop & Focus area"
                        })
                      }),
                      (0, import_jsx_runtime10.jsx)(A, {
                        variant: "pi",
                        textColor: infoMutedColor,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.crop.hint"),
                          defaultMessage: "Set the crop area with the rectangle. Pin the always-visible area with the circle."
                        })
                      })
                    ]
                  }),
                  (0, import_jsx_runtime10.jsxs)(v, {
                    gap: 6,
                    alignItems: "center",
                    children: [
                      (0, import_jsx_runtime10.jsxs)(v, {
                        alignItems: "center",
                        gap: 2,
                        children: [
                          (0, import_jsx_runtime10.jsxs)(v, {
                            direction: "column",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime10.jsxs)(FieldRow, {
                                name: "crop-width",
                                gap: 2,
                                children: [
                                  (0, import_jsx_runtime10.jsx)(LabelIcon, {
                                    textColor: infoTextColor,
                                    children: (0, import_jsx_runtime10.jsx)(Z2, {})
                                  }),
                                  (0, import_jsx_runtime10.jsx)(FieldNumberInput, {
                                    "aria-label": formatMessage({
                                      id: getTranslationKey("asset-details.crop.width"),
                                      defaultMessage: "Width (px)"
                                    }),
                                    value: width,
                                    min: 1,
                                    max: naturalSize.width || void 0,
                                    onValueChange: (next) => {
                                      if (next !== void 0) setCropSize({
                                        width: next
                                      });
                                    }
                                  })
                                ]
                              }),
                              (0, import_jsx_runtime10.jsxs)(FieldRow, {
                                name: "crop-height",
                                gap: 2,
                                children: [
                                  (0, import_jsx_runtime10.jsx)(LabelIcon, {
                                    textColor: infoTextColor,
                                    children: (0, import_jsx_runtime10.jsx)(N2, {})
                                  }),
                                  (0, import_jsx_runtime10.jsx)(FieldNumberInput, {
                                    "aria-label": formatMessage({
                                      id: getTranslationKey("asset-details.crop.height"),
                                      defaultMessage: "Height (px)"
                                    }),
                                    value: height,
                                    min: 1,
                                    max: naturalSize.height || void 0,
                                    onValueChange: (next) => {
                                      if (next !== void 0) setCropSize({
                                        height: next
                                      });
                                    }
                                  })
                                ]
                              })
                            ]
                          }),
                          (0, import_jsx_runtime10.jsxs)(v, {
                            position: "relative",
                            children: [
                              (0, import_jsx_runtime10.jsx)(be, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.crop.aspect-lock"),
                                  defaultMessage: "Lock aspect ratio"
                                }),
                                variant: aspectLocked ? "secondary" : "ghost",
                                onClick: toggleAspectLock,
                                children: (0, import_jsx_runtime10.jsx)(R3, {})
                              }),
                              (0, import_jsx_runtime10.jsx)(BackgroundPreserveRatio, {})
                            ]
                          })
                        ]
                      }),
                      (0, import_jsx_runtime10.jsxs)(v, {
                        direction: "column",
                        gap: 2,
                        marginLeft: "auto",
                        children: [
                          (0, import_jsx_runtime10.jsxs)(FieldRow, {
                            name: "focal-x",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime10.jsx)(Dd.Label, {
                                textColor: infoTextColor,
                                children: formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-x-axis"),
                                  defaultMessage: "X"
                                })
                              }),
                              (0, import_jsx_runtime10.jsx)(FieldNumberInput, {
                                "aria-label": formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-x"),
                                  defaultMessage: "Focal point X (px)"
                                }),
                                value: focalPxX,
                                onValueChange: (next) => {
                                  if (next !== void 0) setFocalPx("x", next);
                                }
                              })
                            ]
                          }),
                          (0, import_jsx_runtime10.jsxs)(FieldRow, {
                            name: "focal-y",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime10.jsx)(Dd.Label, {
                                textColor: infoTextColor,
                                children: formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-y-axis"),
                                  defaultMessage: "Y"
                                })
                              }),
                              (0, import_jsx_runtime10.jsx)(FieldNumberInput, {
                                "aria-label": formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-y"),
                                  defaultMessage: "Focal point Y (px)"
                                }),
                                value: focalPxY,
                                onValueChange: (next) => {
                                  if (next !== void 0) setFocalPx("y", next);
                                }
                              })
                            ]
                          })
                        ]
                      })
                    ]
                  })
                ]
              })
            ]
          }),
          (0, import_jsx_runtime10.jsxs)(FooterBar, {
            alignItems: "center",
            children: [
              (0, import_jsx_runtime10.jsx)(Ct, {
                variant: "tertiary",
                onClick: onClose,
                disabled: isBusy,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime10.jsxs)(v, {
                gap: 2,
                children: [
                  (0, import_jsx_runtime10.jsx)(Ct, {
                    variant: "secondary",
                    onClick: () => handleAction("copy"),
                    loading: isBusy,
                    disabled: !isReady,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.crop.save-as-copy"),
                      defaultMessage: "Save as copy"
                    })
                  }),
                  (0, import_jsx_runtime10.jsx)(Ct, {
                    variant: "default",
                    onClick: () => handleAction("apply"),
                    loading: isBusy,
                    disabled: !isReady,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.crop.apply"),
                      defaultMessage: "Apply"
                    })
                  })
                ]
              })
            ]
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetPreview.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var React18 = __toESM(require_react(), 1);
var PreviewContainer = gt(S)`
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  max-height: 24rem;
  overflow: hidden;
  border-radius: ${({ theme }) => theme.borderRadius};
  padding: ${({ theme }) => theme.spaces[3]};
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var AssetContainer = gt(v)`
  justify-content: center;
  position: relative;
  z-index: 2;
  width: 100%;
  height: 100%;
`;
var StyledImage = gt.img`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var ActionsOverlay = gt(v)`
  position: absolute;
  top: ${({ theme }) => theme.spaces[3]};
  right: ${({ theme }) => theme.spaces[3]};
  z-index: 3;
`;
var StyledVideo = gt.video`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var StyledAudio = gt.audio`
  width: 100%;
`;
var StyledPdfIframe = gt.iframe`
  width: 100%;
  height: 100%;
  min-height: 200px;
  border: none;
`;
var IconFallback = gt(v)`
  height: 100%;
  aspect-ratio: 1;
  width: auto;
  max-width: 100%;
  margin: 0 auto;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral150};
`;
var LoaderOverlay = gt(v)`
  position: absolute;
  inset: 0;
  z-index: 1;
`;
var AssetLoader = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime11.jsx)(LoaderOverlay, {
    justifyContent: "center",
    alignItems: "center",
    children: (0, import_jsx_runtime11.jsx)(kt, {
      children: formatMessage({
        id: "app.loading",
        defaultMessage: "Loading..."
      })
    })
  });
};
var AssetPreview = ({ asset, actions, isLoading = false }) => {
  const { formatMessage } = useIntl();
  const { alternativeText, ext, mime, url, updatedAt, isUrlSigned, isLocal } = asset;
  const cacheKey = updatedAt && !isUrlSigned ? new Date(updatedAt).getTime() : void 0;
  const appendCacheBuster = (raw) => {
    if (!raw || cacheKey === void 0) return raw;
    return raw.includes("?") ? `${raw}&v=${cacheKey}` : `${raw}?v=${cacheKey}`;
  };
  const mediaUrl = appendCacheBuster(prefixFileUrlWithBackendUrl(url));
  const [isMediaLoaded, setIsMediaLoaded] = React18.useState(false);
  React18.useEffect(() => {
    setIsMediaLoaded(false);
  }, [
    mediaUrl
  ]);
  const imageRef = React18.useRef(null);
  React18.useEffect(() => {
    const image = imageRef.current;
    if (!image) return;
    const recompute = () => {
      const parent = image.parentElement;
      if (!parent) return;
      const c = parent.getBoundingClientRect();
      const w = image.offsetWidth;
      const h = image.offsetHeight;
      if (!w || !h || !c.width || !c.height) return;
    };
    recompute();
    const ro = new ResizeObserver(recompute);
    ro.observe(image);
    if (image.parentElement) ro.observe(image.parentElement);
    return () => ro.disconnect();
  }, [
    isMediaLoaded
  ]);
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    const imageUrl = appendCacheBuster(prefixFileUrlWithBackendUrl(url));
    if (imageUrl) {
      return (0, import_jsx_runtime11.jsxs)(PreviewContainer, {
        children: [
          (!isMediaLoaded || isLoading) && (0, import_jsx_runtime11.jsx)(AssetLoader, {}),
          actions ? (0, import_jsx_runtime11.jsx)(ActionsOverlay, {
            children: actions
          }) : null,
          (0, import_jsx_runtime11.jsx)(AssetContainer, {
            children: (0, import_jsx_runtime11.jsx)(StyledImage, {
              ref: imageRef,
              src: imageUrl,
              alt: alternativeText || asset.name || "",
              crossOrigin: !isLocal && isUrlSigned ? "anonymous" : void 0,
              onLoad: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        ]
      });
    }
  }
  if ((mime == null ? void 0 : mime.includes(ASSET_TYPES.Video)) && mediaUrl) {
    return (0, import_jsx_runtime11.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime11.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime11.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime11.jsx)(StyledVideo, {
            src: mediaUrl,
            controls: true,
            title: asset.name,
            onLoadedData: () => setIsMediaLoaded(true),
            onError: () => setIsMediaLoaded(true),
            children: formatMessage({
              id: getTranslationKey("asset-details.videoNotSupported"),
              defaultMessage: "Your browser does not support the video tag."
            })
          })
        })
      ]
    });
  }
  if ((mime == null ? void 0 : mime.includes(ASSET_TYPES.Audio)) && mediaUrl) {
    return (0, import_jsx_runtime11.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime11.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime11.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime11.jsx)(v, {
            width: "100%",
            padding: 4,
            justifyContent: "center",
            alignItems: "center",
            height: "100%",
            minHeight: "12rem",
            children: (0, import_jsx_runtime11.jsx)(StyledAudio, {
              src: mediaUrl,
              controls: true,
              onLoadedData: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        })
      ]
    });
  }
  const isPdf = (ext == null ? void 0 : ext.toLowerCase()) === "pdf" || (ext == null ? void 0 : ext.toLowerCase()) === ".pdf" || mime === "application/pdf";
  if (isPdf && mediaUrl) {
    return (0, import_jsx_runtime11.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime11.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime11.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime11.jsx)(StyledPdfIframe, {
            src: `${mediaUrl}#toolbar=0`,
            title: asset.name,
            onLoad: () => setIsMediaLoaded(true)
          })
        })
      ]
    });
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime11.jsx)(PreviewContainer, {
    children: (0, import_jsx_runtime11.jsxs)(IconFallback, {
      justifyContent: "center",
      alignItems: "center",
      gap: 1,
      direction: "column",
      hasRadius: true,
      children: [
        (0, import_jsx_runtime11.jsx)(DocIcon, {
          width: 24,
          height: 24
        }),
        (0, import_jsx_runtime11.jsx)(A, {
          variant: "pi",
          children: formatMessage({
            id: getTranslationKey("asset-details.noPreview"),
            defaultMessage: "No preview available"
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var URL_PARAM = "assetId";
var DrawerNotifyContext = React19.createContext(null);
var useDrawerNotify = () => {
  const ctx = React19.useContext(DrawerNotifyContext);
  if (!ctx) {
    throw new Error("useDrawerNotify must be used within AssetDetails");
  }
  return ctx;
};
var AssetOperationsContext = React19.createContext(null);
var useAssetOperation = () => {
  const ctx = React19.useContext(AssetOperationsContext);
  if (!ctx) {
    throw new Error("useAssetOperation must be used within AssetDetails");
  }
  return ctx;
};
var useAssetDetailsParam = () => {
  const [{ query }, setQuery] = useQueryParams();
  const detailsId = query == null ? void 0 : query[URL_PARAM];
  const assetId = detailsId ? parseInt(detailsId, 10) : null;
  const hasValidId = assetId !== null && !Number.isNaN(assetId);
  const [isMounted, setIsMounted] = React19.useState(hasValidId);
  const displayAssetId = React19.useRef(null);
  React19.useEffect(() => {
    if (hasValidId) {
      displayAssetId.current = assetId;
      setIsMounted(true);
    }
  }, [
    hasValidId,
    assetId
  ]);
  const onCloseAnimationEnd = React19.useCallback((event) => {
    if (event.target === event.currentTarget && !hasValidId) {
      setIsMounted(false);
    }
  }, [
    hasValidId
  ]);
  const openDetails = React19.useCallback((id) => {
    setQuery({
      [URL_PARAM]: String(id)
    }, "push", true);
  }, [
    setQuery
  ]);
  const closeDetails = React19.useCallback(() => {
    setQuery({
      [URL_PARAM]: void 0
    }, "remove", true);
  }, [
    setQuery
  ]);
  return {
    assetId: hasValidId ? assetId : displayAssetId.current,
    isVisible: hasValidId,
    shouldRenderDrawer: isMounted,
    onCloseAnimationEnd,
    openDetails,
    closeDetails
  };
};
var DetailItemContainer = gt(v)`
  flex: 0 0 calc(50% - ${({ theme }) => theme.spaces[2]});
`;
var DetailItem = ({ label, value }) => (0, import_jsx_runtime12.jsxs)(DetailItemContainer, {
  direction: "column",
  justifyContent: "flex-start",
  alignItems: "flex-start",
  gap: 1,
  children: [
    (0, import_jsx_runtime12.jsx)(A, {
      variant: "sigma",
      textColor: "neutral600",
      fontWeight: "semiBold",
      textTransform: "uppercase",
      children: label
    }),
    (0, import_jsx_runtime12.jsx)(A, {
      variant: "pi",
      textColor: "neutral700",
      children: value ?? "-"
    })
  ]
});
var FormShell = gt(S)`
  display: flex;
  flex-direction: column;
  flex: 1;
  min-height: 0;

  > form {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
    position: relative;
  }
`;
var DrawerToastSlot = gt(S)`
  position: absolute;
  top: ${({ theme }) => theme.spaces[2]};
  left: 50%;
  transform: translateX(-50%);
  z-index: 10;
  width: calc(100% - ${({ theme }) => theme.spaces[2]});
`;
var DrawerBusyOverlay = gt(v)`
  position: absolute;
  inset: 0;
  z-index: 20;
  align-items: center;
  justify-content: center;
  background: ${({ theme }) => theme.colors.neutral0};
  opacity: 0.7;
`;
var getBusyMessage = (state) => {
  if (state.isDeleting) {
    return {
      id: getTranslationKey("asset-details.delete.loading"),
      defaultMessage: "Deleting the file…"
    };
  }
  if (state.isCropCopying) {
    return {
      id: getTranslationKey("asset-details.crop.loading"),
      defaultMessage: "Saving the cropped copy…"
    };
  }
  if (state.isReplacing) {
    return {
      id: getTranslationKey("asset-details.replace.loading"),
      defaultMessage: "Replacing the file…"
    };
  }
  return null;
};
var StyledWarning = gt(Jn)`
  width: 1.6rem;
  height: 1.6rem;

  path {
    fill: ${({ theme }) => theme.colors.warning500};
  }
`;
var DetailField = ({ name, label, required }) => {
  const { formatMessage } = useIntl();
  const field = useField(name);
  const isSubmitting = useForm("DetailField", (state) => state.isSubmitting);
  const value = field.value ?? "";
  const emptyTooltipLabel = formatMessage({
    id: getTranslationKey("asset-details.field.empty"),
    defaultMessage: "{label} is currently empty."
  }, {
    label
  });
  return (0, import_jsx_runtime12.jsxs)(Dd.Root, {
    name,
    required,
    children: [
      (0, import_jsx_runtime12.jsx)(Dd.Label, {
        children: label
      }),
      (0, import_jsx_runtime12.jsx)(Pc, {
        value,
        onChange: (event) => field.onChange(name, event.target.value),
        endAction: !value ? (0, import_jsx_runtime12.jsx)(Mt, {
          label: emptyTooltipLabel,
          children: (0, import_jsx_runtime12.jsx)(StyledWarning, {
            "aria-label": emptyTooltipLabel,
            role: "img"
          })
        }) : void 0,
        type: "text",
        disabled: isSubmitting
      })
    ]
  });
};
var LocationField = ({ label, rootLabel, folders }) => {
  const field = useField("folder");
  const isSubmitting = useForm("LocationField", (state) => state.isSubmitting);
  return (0, import_jsx_runtime12.jsxs)(Dd.Root, {
    name: "folder",
    required: true,
    children: [
      (0, import_jsx_runtime12.jsx)(Dd.Label, {
        children: label
      }),
      (0, import_jsx_runtime12.jsxs)(cr, {
        value: field.value == null ? "" : String(field.value),
        onChange: (value) => {
          const next = value === "" ? null : Number(value);
          field.onChange("folder", next);
        },
        disabled: isSubmitting,
        children: [
          (0, import_jsx_runtime12.jsx)(dr, {
            value: "",
            children: rootLabel
          }),
          folders.map((folder) => (0, import_jsx_runtime12.jsx)(dr, {
            value: String(folder.id),
            children: folder.name
          }, folder.id))
        ]
      })
    ]
  });
};
var DeleteAssetButton = () => {
  const { formatMessage } = useIntl();
  const { deleteAsset, isDeleting } = useAssetOperation();
  const [isOpen, setIsOpen] = React19.useState(false);
  const handleConfirm = async () => {
    await deleteAsset();
    setIsOpen(false);
  };
  const triggerLabel = formatMessage({
    id: getTranslationKey("asset-details.delete.trigger"),
    defaultMessage: "Delete this file"
  });
  return (0, import_jsx_runtime12.jsxs)(Pd.Root, {
    open: isOpen,
    onOpenChange: setIsOpen,
    children: [
      (0, import_jsx_runtime12.jsx)(Pd.Trigger, {
        children: (0, import_jsx_runtime12.jsx)(be, {
          withTooltip: false,
          label: triggerLabel,
          variant: "danger-light",
          children: (0, import_jsx_runtime12.jsx)(In, {})
        })
      }),
      (0, import_jsx_runtime12.jsxs)(Pd.Content, {
        children: [
          (0, import_jsx_runtime12.jsx)(Pd.Header, {
            children: formatMessage({
              id: getTranslationKey("asset-details.delete.title"),
              defaultMessage: "Delete this media file?"
            })
          }),
          (0, import_jsx_runtime12.jsx)(Pd.Body, {
            icon: (0, import_jsx_runtime12.jsx)(Jn, {
              width: "24px",
              height: "24px",
              fill: "danger600"
            }),
            textAlign: "center",
            children: formatMessage({
              id: getTranslationKey("asset-details.delete.description"),
              defaultMessage: "This file cannot be recovered once deleted. If it is currently in use, linked content will break and image containers will be empty."
            })
          }),
          (0, import_jsx_runtime12.jsxs)(Pd.Footer, {
            children: [
              (0, import_jsx_runtime12.jsx)(Pd.Cancel, {
                children: (0, import_jsx_runtime12.jsx)(Ct, {
                  variant: "tertiary",
                  disabled: isDeleting,
                  fullWidth: true,
                  children: formatMessage({
                    id: "app.components.Button.cancel",
                    defaultMessage: "Cancel"
                  })
                })
              }),
              (0, import_jsx_runtime12.jsx)(Pd.Action, {
                children: (0, import_jsx_runtime12.jsx)(Ct, {
                  variant: "danger-light",
                  loading: isDeleting,
                  onClick: handleConfirm,
                  fullWidth: true,
                  children: formatMessage({
                    id: "app.components.Button.confirm",
                    defaultMessage: "Confirm"
                  })
                })
              })
            ]
          })
        ]
      })
    ]
  });
};
var CopyLinkButton = ({ asset }) => {
  const { formatMessage } = useIntl();
  const { copy } = useClipboard();
  const notify = useDrawerNotify();
  const handleCopy = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) return;
    const didCopy = await copy(url);
    notify({
      type: didCopy ? "success" : "danger",
      message: didCopy ? formatMessage({
        id: getTranslationKey("asset-details.copy-link.success"),
        defaultMessage: "Link copied."
      }) : formatMessage({
        id: getTranslationKey("asset-details.copy-link.error"),
        defaultMessage: "Failed to copy the link."
      })
    });
  };
  return (0, import_jsx_runtime12.jsx)(be, {
    withTooltip: false,
    label: formatMessage({
      id: getTranslationKey("asset-details.copy-link.trigger"),
      defaultMessage: "Copy link"
    }),
    variant: "tertiary",
    onClick: handleCopy,
    children: (0, import_jsx_runtime12.jsx)(R3, {})
  });
};
var DownloadAssetButton = ({ asset }) => {
  const { formatMessage } = useIntl();
  const notify = useDrawerNotify();
  const [isDownloading, setIsDownloading] = React19.useState(false);
  const handleDownload = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) return;
    setIsDownloading(true);
    try {
      await downloadFile(url, asset.name);
    } catch {
      notify({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.download.error"),
          defaultMessage: "Failed to download the file."
        })
      });
    } finally {
      setIsDownloading(false);
    }
  };
  return (0, import_jsx_runtime12.jsx)(be, {
    withTooltip: false,
    label: formatMessage({
      id: getTranslationKey("asset-details.download.trigger"),
      defaultMessage: "Download"
    }),
    variant: "tertiary",
    onClick: handleDownload,
    disabled: isDownloading,
    children: (0, import_jsx_runtime12.jsx)(z5, {})
  });
};
var ReplaceAssetButton = () => {
  var _a;
  const { formatMessage } = useIntl();
  const { replaceAsset, isReplacing } = useAssetOperation();
  const fileInputRef = React19.useRef(null);
  const [isDialogOpen, setIsDialogOpen] = React19.useState(false);
  const { data: settings } = useGetSettingsQuery();
  const aiEnabled = ((_a = settings == null ? void 0 : settings.data) == null ? void 0 : _a.aiMetadata) ?? false;
  const handleTriggerClick = () => {
    setIsDialogOpen(true);
  };
  const handleContinue = () => {
    var _a2;
    setIsDialogOpen(false);
    (_a2 = fileInputRef.current) == null ? void 0 : _a2.click();
  };
  const handleFileChange = async (event) => {
    var _a2;
    const file = (_a2 = event.target.files) == null ? void 0 : _a2[0];
    event.target.value = "";
    if (!file) {
      return;
    }
    await replaceAsset(file);
  };
  return (0, import_jsx_runtime12.jsxs)(import_jsx_runtime12.Fragment, {
    children: [
      (0, import_jsx_runtime12.jsx)($e, {
        children: (0, import_jsx_runtime12.jsx)("input", {
          ref: fileInputRef,
          type: "file",
          multiple: false,
          onChange: handleFileChange,
          "aria-hidden": true,
          tabIndex: -1
        })
      }),
      (0, import_jsx_runtime12.jsx)(be, {
        withTooltip: false,
        label: formatMessage({
          id: getTranslationKey("asset-details.replace.trigger"),
          defaultMessage: "Replace this file"
        }),
        variant: "tertiary",
        onClick: handleTriggerClick,
        disabled: isReplacing,
        children: (0, import_jsx_runtime12.jsx)(W2, {})
      }),
      (0, import_jsx_runtime12.jsx)(Pd.Root, {
        open: isDialogOpen,
        onOpenChange: setIsDialogOpen,
        children: (0, import_jsx_runtime12.jsxs)(Pd.Content, {
          children: [
            (0, import_jsx_runtime12.jsx)(Pd.Header, {
              children: formatMessage({
                id: getTranslationKey("asset-details.replace.title"),
                defaultMessage: "Replace this media file?"
              })
            }),
            (0, import_jsx_runtime12.jsx)(Pd.Body, {
              textAlign: "center",
              children: (0, import_jsx_runtime12.jsxs)(v, {
                direction: "column",
                textAlign: "center",
                children: [
                  (0, import_jsx_runtime12.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description"),
                      defaultMessage: "Current content will be permanently replaced."
                    })
                  }),
                  aiEnabled ? (0, import_jsx_runtime12.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description.ai"),
                      defaultMessage: "AI will generate new metadata after upload."
                    })
                  }) : null
                ]
              })
            }),
            (0, import_jsx_runtime12.jsxs)(Pd.Footer, {
              children: [
                (0, import_jsx_runtime12.jsx)(Pd.Cancel, {
                  children: (0, import_jsx_runtime12.jsx)(Ct, {
                    variant: "tertiary",
                    fullWidth: true,
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  })
                }),
                (0, import_jsx_runtime12.jsx)(Pd.Action, {
                  children: (0, import_jsx_runtime12.jsx)(Ct, {
                    variant: "secondary",
                    onClick: handleContinue,
                    fullWidth: true,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.continue"),
                      defaultMessage: "Continue"
                    })
                  })
                })
              ]
            })
          ]
        })
      })
    ]
  });
};
var AssetImageActions = ({ onCrop }) => {
  const { formatMessage } = useIntl();
  const isSubmitting = useForm("AssetImageActions", (state) => state.isSubmitting);
  return (0, import_jsx_runtime12.jsxs)(v, {
    direction: "column",
    gap: 2,
    children: [
      (0, import_jsx_runtime12.jsx)(be, {
        withTooltip: false,
        label: formatMessage({
          id: getTranslationKey("asset-details.crop.trigger"),
          defaultMessage: "Crop"
        }),
        variant: "tertiary",
        onClick: onCrop,
        disabled: isSubmitting || !onCrop,
        children: (0, import_jsx_runtime12.jsx)(S5, {})
      }),
      (0, import_jsx_runtime12.jsx)(ReplaceAssetButton, {})
    ]
  });
};
var AssetDetails = ({ asset, closeDetails }) => {
  var _a;
  const { formatMessage, formatDate } = useIntl();
  const { data: folders = [] } = useGetAllFoldersQuery();
  const { toggleNotification } = useNotification();
  const [updateAsset] = useUpdateAssetMutation();
  const [replaceMutation, { isLoading: isReplacing }] = useReplaceAssetMutation();
  const [deleteMutation, { isLoading: isDeleting }] = useDeleteAssetMutation();
  const [uploadCroppedCopy, { isLoading: isCropCopying }] = useUploadFileSilentlyMutation();
  const [isCropOpen, setIsCropOpen] = React19.useState(false);
  const [drawerToast, setDrawerToast] = React19.useState(null);
  React19.useEffect(() => {
    if (!drawerToast) return;
    const timer = window.setTimeout(() => setDrawerToast(null), 5e3);
    return () => window.clearTimeout(timer);
  }, [
    drawerToast
  ]);
  const notify = React19.useCallback((toast) => setDrawerToast(toast), []);
  const isImage = (_a = asset.mime) == null ? void 0 : _a.includes(ASSET_TYPES.Image);
  const initialValues = {
    name: asset.name ?? "",
    caption: asset.caption ?? "",
    alternativeText: asset.alternativeText ?? "",
    folder: typeof asset.folder === "object" && asset.folder !== null ? asset.folder.id ?? null : asset.folder ?? null
  };
  const handleSubmit = async (values) => {
    const fileInfo = {
      name: values.name,
      caption: values.caption,
      alternativeText: values.alternativeText,
      folder: values.folder
    };
    const res = await updateAsset({
      id: asset.id,
      fileInfo
    });
    if ("error" in res) {
      notify({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.update.error"),
          defaultMessage: "Failed to update the file."
        })
      });
      return;
    }
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.update.success"),
        defaultMessage: "File updated"
      })
    });
  };
  const { title: folderName } = useFolderInfo(typeof asset.folder === "object" && asset.folder !== null ? asset.folder.id ?? null : asset.folder ?? null);
  const handleReplace = async (file) => {
    var _a2, _b, _c;
    const res = await replaceMutation({
      id: asset.id,
      file
    });
    if ("error" in res) {
      const error = res.error;
      const message = ((_b = (_a2 = error == null ? void 0 : error.data) == null ? void 0 : _a2.error) == null ? void 0 : _b.message) ?? ((_c = error == null ? void 0 : error.data) == null ? void 0 : _c.message) ?? formatMessage({
        id: getTranslationKey("asset-details.replace.error"),
        defaultMessage: "Failed to replace the file."
      });
      notify({
        type: "danger",
        message
      });
      return;
    }
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.replace.success"),
        defaultMessage: "File replaced."
      })
    });
  };
  const handleDelete = async () => {
    var _a2, _b, _c;
    const res = await deleteMutation(asset.id);
    if ("error" in res) {
      const error = res.error;
      const message = ((_b = (_a2 = error == null ? void 0 : error.data) == null ? void 0 : _a2.error) == null ? void 0 : _b.message) ?? ((_c = error == null ? void 0 : error.data) == null ? void 0 : _c.message) ?? formatMessage({
        id: getTranslationKey("asset-details.delete.error"),
        defaultMessage: "Failed to delete the asset."
      });
      notify({
        type: "danger",
        message
      });
      return;
    }
    toggleNotification({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.delete.success"),
        defaultMessage: "1 element have been deleted from {folderName}"
      }, {
        folderName
      })
    });
    closeDetails();
  };
  const notifyCropError = () => {
    notify({
      type: "danger",
      message: formatMessage({
        id: getTranslationKey("asset-details.crop.error"),
        defaultMessage: "Failed to crop the file."
      })
    });
  };
  const handleCropApply = async (file, focalPoint) => {
    setIsCropOpen(false);
    const res = await replaceMutation({
      id: asset.id,
      file,
      fileInfo: {
        focalPoint
      }
    });
    if ("error" in res) {
      notifyCropError();
      return;
    }
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.crop.success"),
        defaultMessage: "File cropped."
      })
    });
  };
  const handleCropSaveAsCopy = async (file, focalPoint) => {
    setIsCropOpen(false);
    const res = await uploadCroppedCopy({
      file,
      fileInfo: {
        name: asset.name,
        caption: asset.caption ?? "",
        alternativeText: asset.alternativeText ?? "",
        folder: initialValues.folder,
        focalPoint
      }
    });
    if ("error" in res) {
      notifyCropError();
      return;
    }
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.crop.copy-success"),
        defaultMessage: "Copy created."
      })
    });
  };
  const operations = React19.useMemo(
    () => ({
      replaceAsset: handleReplace,
      deleteAsset: handleDelete,
      isReplacing,
      isDeleting
    }),
    // handleReplace / handleDelete close over asset+mutations and don't need a
    // stable identity here; the consumers re-render with the new context value.
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      isReplacing,
      isDeleting
    ]
  );
  return (
    // `key={asset.id}` resets the form when the drawer switches to a different
    // asset so cached values from the previous asset don't bleed in.
    (0, import_jsx_runtime12.jsx)(DrawerNotifyContext.Provider, {
      value: notify,
      children: (0, import_jsx_runtime12.jsx)(AssetOperationsContext.Provider, {
        value: operations,
        children: (0, import_jsx_runtime12.jsx)(FormShell, {
          children: (0, import_jsx_runtime12.jsx)(Form, {
            method: "POST",
            initialValues,
            onSubmit: handleSubmit,
            children: ({ modified, isSubmitting, values, resetForm }) => {
              const nameIsEmpty = (values.name ?? "").trim() === "";
              const busyMessage = getBusyMessage({
                isDeleting,
                isReplacing,
                isCropCopying
              });
              return (0, import_jsx_runtime12.jsxs)(import_jsx_runtime12.Fragment, {
                children: [
                  (0, import_jsx_runtime12.jsx)(Blocker, {
                    onProceed: resetForm
                  }),
                  isCropOpen && isImage ? (0, import_jsx_runtime12.jsx)(AssetCropEditor, {
                    asset,
                    onClose: () => setIsCropOpen(false),
                    onApply: handleCropApply,
                    onSaveAsCopy: handleCropSaveAsCopy
                  }) : null,
                  busyMessage ? (0, import_jsx_runtime12.jsx)(DrawerBusyOverlay, {
                    children: (0, import_jsx_runtime12.jsx)(kt, {
                      children: formatMessage(busyMessage)
                    })
                  }) : null,
                  drawerToast ? (0, import_jsx_runtime12.jsx)(DrawerToastSlot, {
                    children: (0, import_jsx_runtime12.jsx)(ud, {
                      variant: drawerToast.type === "success" ? "success" : "danger",
                      closeLabel: formatMessage({
                        id: "global.close",
                        defaultMessage: "Close"
                      }),
                      onClose: () => setDrawerToast(null),
                      children: drawerToast.message
                    })
                  }) : null,
                  (0, import_jsx_runtime12.jsxs)(Drawer.ScrollableContent, {
                    children: [
                      (0, import_jsx_runtime12.jsx)(AssetPreview, {
                        asset,
                        actions: isImage ? (0, import_jsx_runtime12.jsx)(AssetImageActions, {
                          onCrop: () => setIsCropOpen(true)
                        }) : null
                      }),
                      (0, import_jsx_runtime12.jsxs)(v, {
                        direction: "column",
                        alignItems: "stretch",
                        gap: 4,
                        paddingTop: 4,
                        paddingBottom: 4,
                        paddingLeft: 5,
                        paddingRight: 5,
                        children: [
                          (0, import_jsx_runtime12.jsx)(A, {
                            variant: "beta",
                            fontWeight: "semiBold",
                            tag: "h3",
                            children: formatMessage({
                              id: getTranslationKey("asset-details.fileInfo"),
                              defaultMessage: "File info"
                            })
                          }),
                          (0, import_jsx_runtime12.jsxs)(v, {
                            wrap: "wrap",
                            gap: 4,
                            background: "neutral100",
                            paddingTop: 4,
                            paddingBottom: 4,
                            paddingLeft: 6,
                            paddingRight: 6,
                            alignItems: "flex-start",
                            children: [
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.creationDate"),
                                  defaultMessage: "Creation date"
                                }),
                                value: asset.createdAt ? formatDate(new Date(asset.createdAt), {
                                  dateStyle: "long",
                                  timeStyle: "short"
                                }) : null
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.lastUpdated"),
                                  defaultMessage: "Last updated"
                                }),
                                value: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
                                  dateStyle: "long",
                                  timeStyle: "short"
                                }) : null
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.createdBy"),
                                  defaultMessage: "Created by"
                                }),
                                value: asset.createdBy ? getDisplayName({
                                  firstname: asset.createdBy.firstname ?? void 0,
                                  lastname: asset.createdBy.lastname ?? void 0,
                                  username: asset.createdBy.username ?? void 0,
                                  email: asset.createdBy.email ?? void 0
                                }) ?? "-" : null
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.size"),
                                  defaultMessage: "Size"
                                }),
                                value: asset.size ? formatBytes(asset.size, 1) : null
                              }),
                              isImage && (asset.width != null || asset.height != null) && (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.dimensions"),
                                  defaultMessage: "Dimensions"
                                }),
                                value: asset.width != null && asset.height != null ? `${asset.width} × ${asset.height}` : null
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.extension"),
                                  defaultMessage: "Extension"
                                }),
                                value: getFileExtension(asset.ext)
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.assetId"),
                                  defaultMessage: "Asset ID"
                                }),
                                value: String(asset.id)
                              })
                            ]
                          }),
                          (0, import_jsx_runtime12.jsx)(DetailField, {
                            name: "name",
                            label: formatMessage({
                              id: getTranslationKey("asset-details.fileName"),
                              defaultMessage: "File name"
                            }),
                            required: true
                          }),
                          (0, import_jsx_runtime12.jsx)(LocationField, {
                            label: formatMessage({
                              id: getTranslationKey("asset-details.location"),
                              defaultMessage: "Location"
                            }),
                            rootLabel: formatMessage({
                              id: getTranslationKey("plugin.home"),
                              defaultMessage: "Home"
                            }),
                            folders
                          }),
                          isImage && (0, import_jsx_runtime12.jsxs)(import_jsx_runtime12.Fragment, {
                            children: [
                              (0, import_jsx_runtime12.jsx)(DetailField, {
                                name: "caption",
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.caption"),
                                  defaultMessage: "Caption"
                                })
                              }),
                              (0, import_jsx_runtime12.jsx)(DetailField, {
                                name: "alternativeText",
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.alternativeText"),
                                  defaultMessage: "Alternative text"
                                })
                              })
                            ]
                          })
                        ]
                      })
                    ]
                  }),
                  (0, import_jsx_runtime12.jsxs)(v, {
                    justifyContent: "space-between",
                    alignItems: "center",
                    gap: 2,
                    padding: 3,
                    borderColor: "neutral150",
                    borderStyle: "solid",
                    borderWidth: "1px 0 0 0",
                    background: "neutral0",
                    children: [
                      (0, import_jsx_runtime12.jsxs)(v, {
                        gap: 2,
                        children: [
                          (0, import_jsx_runtime12.jsx)(DeleteAssetButton, {}),
                          (0, import_jsx_runtime12.jsx)(CopyLinkButton, {
                            asset
                          }),
                          (0, import_jsx_runtime12.jsx)(DownloadAssetButton, {
                            asset
                          })
                        ]
                      }),
                      (0, import_jsx_runtime12.jsx)(Ct, {
                        type: "submit",
                        variant: "default",
                        loading: isSubmitting,
                        // File name is required; block submit when it's empty or whitespace so the API can't 400 on a blank value.
                        disabled: !modified || isSubmitting || nameIsEmpty,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.save"),
                          defaultMessage: "Save changes"
                        })
                      })
                    ]
                  })
                ]
              });
            }
          }, asset.id)
        })
      })
    })
  );
};
var DrawerHeader = ({ asset, closeDetails }) => {
  const DocIcon = asset ? getAssetIcon(asset.mime, asset.ext) : N5;
  return (0, import_jsx_runtime12.jsxs)(v, {
    gap: 2,
    paddingLeft: 5,
    paddingTop: 3,
    paddingBottom: 3,
    paddingRight: 3,
    borderColor: "neutral150",
    borderStyle: "solid",
    borderWidth: "0 0 1px 0",
    children: [
      (0, import_jsx_runtime12.jsx)(DocIcon, {
        width: 20,
        height: 20
      }),
      (0, import_jsx_runtime12.jsx)(Drawer.Title, {
        asChild: true,
        children: (0, import_jsx_runtime12.jsx)(A, {
          variant: "omega",
          fontWeight: "semiBold",
          overflow: "hidden",
          ellipsis: true,
          tag: "h2",
          children: asset.name
        })
      }),
      (0, import_jsx_runtime12.jsx)(S, {
        marginLeft: "auto",
        children: (0, import_jsx_runtime12.jsx)(Drawer.CloseButton, {
          onClose: closeDetails,
          children: (0, import_jsx_runtime12.jsx)(O2, {})
        })
      })
    ]
  });
};
var DrawerContent = ({ assetId, closeDetails }) => {
  const { formatMessage } = useIntl();
  const { data: asset, isLoading, error } = useGetAssetQuery(assetId, {
    refetchOnMountOrArgChange: false,
    refetchOnReconnect: false,
    refetchOnFocus: false
  });
  if (isLoading) {
    return (0, import_jsx_runtime12.jsx)(v, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime12.jsx)(kt, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error || !asset) {
    return (0, import_jsx_runtime12.jsx)(v, {
      direction: "column",
      alignItems: "stretch",
      gap: 4,
      padding: 4,
      children: (0, import_jsx_runtime12.jsx)(ud, {
        variant: "danger",
        closeLabel: formatMessage({
          id: "global.close",
          defaultMessage: "Close"
        }),
        onClose: closeDetails,
        children: formatMessage({
          id: getTranslationKey("asset-details.error"),
          defaultMessage: "Failed to load file details."
        })
      })
    });
  }
  return (0, import_jsx_runtime12.jsxs)(import_jsx_runtime12.Fragment, {
    children: [
      (0, import_jsx_runtime12.jsx)(DrawerHeader, {
        asset,
        closeDetails
      }),
      (0, import_jsx_runtime12.jsx)(AssetDetails, {
        asset,
        closeDetails
      })
    ]
  });
};
var AssetDetailsDrawer = () => {
  const { formatMessage } = useIntl();
  const { assetId, isVisible, shouldRenderDrawer, onCloseAnimationEnd, closeDetails } = useAssetDetailsParam();
  if (!shouldRenderDrawer || assetId === null) {
    return null;
  }
  return (0, import_jsx_runtime12.jsxs)(Drawer.Root, {
    isVisible,
    onClose: closeDetails,
    children: [
      (0, import_jsx_runtime12.jsx)("div", {
        children: (0, import_jsx_runtime12.jsxs)($e, {
          children: [
            (0, import_jsx_runtime12.jsx)(Drawer.Title, {
              children: formatMessage({
                id: getTranslationKey("asset-details.title"),
                defaultMessage: "File details"
              })
            }),
            (0, import_jsx_runtime12.jsx)(Drawer.Description, {
              children: formatMessage({
                id: getTranslationKey("asset-details.description"),
                defaultMessage: "Displays file information and metadata"
              })
            })
          ]
        })
      }),
      (0, import_jsx_runtime12.jsx)(Drawer.Body, {
        animationDirection: "left",
        width: "41.6rem",
        height: "100vh",
        onAnimationEnd: onCloseAnimationEnd,
        children: (0, import_jsx_runtime12.jsx)(DrawerContent, {
          assetId,
          closeDetails
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderNavigation.mjs
var import_react4 = __toESM(require_react(), 1);
var parseFolderIdFromQuery = (folder) => {
  if (!folder) {
    return null;
  }
  const id = Number(folder);
  return Number.isFinite(id) ? id : null;
};
var useFolderNavigation = () => {
  const [{ query }, setQuery] = useQueryParams();
  const currentFolderId = parseFolderIdFromQuery(query == null ? void 0 : query.folder);
  const navigateToFolder = (folder) => {
    setQuery({
      folder: String(folder.id)
    });
  };
  const navigateToRoot = (0, import_react4.useCallback)(() => {
    setQuery({
      folder: ""
    }, "remove");
  }, [
    setQuery
  ]);
  (0, import_react4.useEffect)(() => {
    if ((query == null ? void 0 : query.folder) && currentFolderId === null) {
      navigateToRoot();
    }
  }, [
    query == null ? void 0 : query.folder,
    currentFolderId,
    navigateToRoot
  ]);
  const navigateToFolderId = (folderId) => {
    if (folderId == null) {
      navigateToRoot();
    } else {
      setQuery({
        folder: String(folderId)
      });
    }
  };
  return {
    currentFolderId,
    navigateToFolder,
    navigateToRoot,
    navigateToFolderId
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/AssetsDndProvider.mjs
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/buildBulkMovePayload.mjs
var buildBulkMovePayload = (items) => {
  const fileIds = [];
  const folderIds = [];
  for (const item of items) {
    if (item.kind === "file") {
      fileIds.push(item.id);
    } else {
      folderIds.push(item.id);
    }
  }
  return {
    fileIds,
    folderIds
  };
};

// node_modules/@strapi/upload/dist/admin/future/utils/canDropItemOnFolder.mjs
var findNode = (nodes, id) => {
  for (const node of nodes) {
    if (node.id === id) {
      return node;
    }
    const found = findNode(node.children, id);
    if (found) {
      return found;
    }
  }
  return null;
};
var collectDescendantIds = (node) => {
  const ids = /* @__PURE__ */ new Set();
  const walk = (current) => {
    for (const child of current.children) {
      if (child.id != null) {
        ids.add(child.id);
      }
      walk(child);
    }
  };
  walk(node);
  return ids;
};
var isFolderDescendantOf = (folderStructure, ancestorId, candidateId) => {
  if (ancestorId === candidateId) {
    return true;
  }
  const ancestorNode = findNode(folderStructure, ancestorId);
  if (!ancestorNode) {
    return false;
  }
  return collectDescendantIds(ancestorNode).has(candidateId);
};
var canDropItemOnFolder = ({ items, targetFolderId, folderStructure }) => {
  if (items.length === 0) {
    return false;
  }
  const draggedFolderIds = new Set(items.filter((item) => item.kind === "folder").map((item) => item.id));
  if (draggedFolderIds.has(targetFolderId)) {
    return false;
  }
  for (const folderId of draggedFolderIds) {
    if (isFolderDescendantOf(folderStructure, folderId, targetFolderId)) {
      return false;
    }
  }
  for (const item of items) {
    if (item.kind === "file" && item.folderId === targetFolderId) {
      return false;
    }
    if (item.kind === "folder" && item.parentId === targetFolderId) {
      return false;
    }
  }
  return true;
};

// node_modules/@strapi/upload/dist/admin/future/utils/getBulkMoveErrorMessage.mjs
var isNonEmptyString = (value) => typeof value === "string" && value.length > 0;
var getBulkMoveErrorMessage = (error, fallback) => {
  var _a, _b, _c;
  if (!error || typeof error !== "object") {
    return fallback;
  }
  const rtkError = error;
  const candidates = [
    (_b = (_a = rtkError.data) == null ? void 0 : _a.error) == null ? void 0 : _b.message,
    (_c = rtkError.data) == null ? void 0 : _c.message,
    rtkError.message
  ];
  for (const candidate of candidates) {
    if (isNonEmptyString(candidate)) {
      return candidate;
    }
  }
  return fallback;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/dndIds.mjs
var toFileDraggableId = (id) => `file:${id}`;
var toFolderDraggableId = (id) => `folder:${id}`;
var toFolderTargetId = (id) => `folder-target:${id}`;
var parseFolderTargetId = (dndId) => {
  if (typeof dndId !== "string") {
    return null;
  }
  const match = /^folder-target:(\d+)$/.exec(dndId);
  return match ? Number(match[1]) : null;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/DragOverlayChip.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var Chip = gt(v)`
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.primary100};
  box-shadow: ${({ theme }) => theme.shadows.tableShadow};
  cursor: grabbing;
  max-width: 24rem;
`;
var DragOverlayChip = ({ item }) => {
  const Icon = item.kind === "folder" ? _5 : Z5;
  return (0, import_jsx_runtime13.jsxs)(Chip, {
    children: [
      (0, import_jsx_runtime13.jsx)(Icon, {
        width: 20,
        height: 20
      }),
      (0, import_jsx_runtime13.jsx)(A, {
        textColor: "neutral800",
        fontWeight: "semiBold",
        ellipsis: true,
        children: item.name
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/AssetsDndProvider.mjs
var AssetsDndContext = (0, import_react5.createContext)(null);
var useAssetsDndOptional = () => (0, import_react5.useContext)(AssetsDndContext);
var getDragItems = (activeData) => {
  if (!activeData) {
    return [];
  }
  return [
    activeData
  ];
};
var AssetsDndProvider = ({ children }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const { data: folderStructure = [] } = useGetFolderStructureQuery();
  const [bulkMove, { isLoading: isMovePending }] = useBulkMoveMutation();
  const [activeItem, setActiveItem] = (0, import_react5.useState)(null);
  const [liveAnnouncement, setLiveAnnouncement] = (0, import_react5.useState)("");
  const announceToLiveRegion = (0, import_react5.useCallback)((message) => {
    setLiveAnnouncement("");
    requestAnimationFrame(() => setLiveAnnouncement(message));
  }, []);
  const sensors = useSensors(useSensor(PointerSensor, {
    activationConstraint: {
      distance: 8
    }
  }));
  const contextValue = (0, import_react5.useMemo)(() => ({
    isInternalDragActive: activeItem !== null,
    isMovePending
  }), [
    activeItem,
    isMovePending
  ]);
  const handleDragStart = (0, import_react5.useCallback)((event) => {
    const data = event.active.data.current;
    setActiveItem(data ?? null);
  }, []);
  const handleDragEnd = (0, import_react5.useCallback)(async (event) => {
    const { active, over } = event;
    setActiveItem(null);
    if (isMovePending || !over) {
      return;
    }
    const targetFolderId = parseFolderTargetId(over.id);
    const activeData = active.data.current;
    if (targetFolderId == null || !activeData) {
      return;
    }
    const items = getDragItems(activeData);
    if (!canDropItemOnFolder({
      items,
      targetFolderId,
      folderStructure
    })) {
      return;
    }
    const payload = buildBulkMovePayload(items);
    const successMessage = formatMessage({
      id: getTranslationKey("modal.move.success-label"),
      defaultMessage: "Elements have been moved successfully."
    });
    const errorFallback = formatMessage({
      id: getTranslationKey("modal.move.error-label"),
      defaultMessage: "An error occurred while moving the elements."
    });
    try {
      await bulkMove({
        ...payload,
        destinationFolderId: targetFolderId
      }).unwrap();
      announceToLiveRegion(successMessage);
      toggleNotification({
        type: "success",
        message: successMessage
      });
    } catch (error) {
      const errorMessage = getBulkMoveErrorMessage(error, errorFallback);
      announceToLiveRegion(formatMessage({
        id: getTranslationKey("dnd.announce.move-failure"),
        defaultMessage: "Move failed. {message}"
      }, {
        message: errorMessage
      }));
      toggleNotification({
        type: "danger",
        message: errorMessage
      });
    }
  }, [
    announceToLiveRegion,
    bulkMove,
    folderStructure,
    formatMessage,
    isMovePending,
    toggleNotification
  ]);
  const handleDragCancel = (0, import_react5.useCallback)(() => {
    setActiveItem(null);
  }, []);
  const announcements = (0, import_react5.useMemo)(() => ({
    onDragStart: ({ active }) => {
      const data = active.data.current;
      return data ? formatMessage({
        id: getTranslationKey("dnd.announce.drag-start"),
        defaultMessage: "Picked up {name}. Drop on a folder to move."
      }, {
        name: data.name
      }) : "";
    },
    onDragOver: () => "",
    onDragEnd: ({ active, over }) => {
      if (!over) {
        return formatMessage({
          id: getTranslationKey("dnd.announce.cancel"),
          defaultMessage: "Drag cancelled."
        });
      }
      const targetFolderId = parseFolderTargetId(over.id);
      const activeData = active.data.current;
      if (targetFolderId == null || !activeData) {
        return "";
      }
      const items = getDragItems(activeData);
      if (!canDropItemOnFolder({
        items,
        targetFolderId,
        folderStructure
      })) {
        return formatMessage({
          id: getTranslationKey("dnd.announce.invalid-drop"),
          defaultMessage: "Cannot move item to this folder."
        });
      }
      return "";
    },
    onDragCancel: () => formatMessage({
      id: getTranslationKey("dnd.announce.cancel"),
      defaultMessage: "Drag cancelled."
    })
  }), [
    folderStructure,
    formatMessage
  ]);
  return (0, import_jsx_runtime14.jsx)(AssetsDndContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime14.jsxs)(DndContext, {
      sensors,
      collisionDetection: pointerWithin,
      onDragStart: handleDragStart,
      onDragEnd: handleDragEnd,
      onDragCancel: handleDragCancel,
      accessibility: {
        announcements
      },
      children: [
        (0, import_jsx_runtime14.jsx)($e, {
          "aria-live": "polite",
          "aria-atomic": "true",
          children: liveAnnouncement
        }),
        (0, import_jsx_runtime14.jsx)(S, {
          position: "relative",
          children
        }),
        (0, import_jsx_runtime14.jsx)(DragOverlay, {
          dropAnimation: null,
          children: activeItem ? (0, import_jsx_runtime14.jsx)(DragOverlayChip, {
            item: activeItem
          }) : null
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/useAssetDnd.mjs
var import_react6 = __toESM(require_react(), 1);
var useFileDraggable = (asset) => {
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const data = (0, import_react6.useMemo)(() => ({
    kind: "file",
    id: asset.id,
    name: asset.name,
    folderId: asset.folder == null ? null : typeof asset.folder === "number" ? asset.folder : Number(asset.folder) || null
  }), [
    asset.folder,
    asset.id,
    asset.name
  ]);
  return useDraggable({
    id: toFileDraggableId(asset.id),
    data,
    disabled: isMovePending
  });
};
var useFolderDraggableDroppable = (folder) => {
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { active } = useDndContext();
  const { data: folderStructure = [] } = useGetFolderStructureQuery();
  const parentId = typeof folder.parent === "object" && folder.parent != null ? folder.parent.id ?? null : folder.parent ?? null;
  const dragData = (0, import_react6.useMemo)(() => ({
    kind: "folder",
    id: folder.id,
    name: folder.name,
    parentId
  }), [
    folder.id,
    folder.name,
    parentId
  ]);
  const dropData = (0, import_react6.useMemo)(() => ({
    kind: "folder-target",
    id: folder.id,
    name: folder.name
  }), [
    folder.id,
    folder.name
  ]);
  const draggable = useDraggable({
    id: toFolderDraggableId(folder.id),
    data: dragData,
    disabled: isMovePending
  });
  const droppable = useDroppable({
    id: toFolderTargetId(folder.id),
    data: dropData,
    disabled: isMovePending
  });
  const activeData = active == null ? void 0 : active.data.current;
  const isValidDropTarget = (0, import_react6.useMemo)(() => {
    if (!activeData || activeData.id === folder.id && activeData.kind === "folder") {
      return false;
    }
    return canDropItemOnFolder({
      items: [
        activeData
      ],
      targetFolderId: folder.id,
      folderStructure
    });
  }, [
    activeData,
    folder.id,
    folderStructure
  ]);
  const isOver = droppable.isOver;
  const showValidDropHighlight = isOver && isValidDropTarget;
  const showInvalidDropCursor = isOver && !isValidDropTarget && active != null;
  return {
    dragData,
    draggable,
    droppable,
    isDragging: draggable.isDragging,
    showValidDropHighlight,
    showInvalidDropCursor
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var StyledCard = gt(md)`
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: 8px;
  overflow: hidden;
  cursor: ${({ $isMovePending }) => $isMovePending ? "wait" : "pointer"};
  opacity: ${({ $isDragging }) => $isDragging ? 0.4 : 1};
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FoldersRow = gt(S)`
  grid-column: 1 / -1;
`;
var StyledFolderCard = gt(v)`
  width: 100%;
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`}; // 8px 12px
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]}; // 8px
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: ${({ $isMovePending, $isInvalidDropTarget }) => {
  if ($isMovePending) {
    return "wait";
  }
  return $isInvalidDropTarget ? "not-allowed" : "pointer";
}};
  opacity: ${({ $isDragging }) => $isDragging ? 0.4 : 1};
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};
  transition: background 0.2s;

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      background: ${theme.colors.primary100};
      border: 1px dashed ${theme.colors.primary600};
    `}

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FolderIconContainer = gt(v)`
  flex-shrink: 0;
  color: ${({ theme }) => theme.colors.neutral600};
`;
var FolderName = gt(A)`
  flex: 1;
  min-width: 0;
`;
var FolderCard = ({ folder }) => {
  const { formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { draggable: { attributes, listeners, setNodeRef: setDragRef, isDragging }, droppable: { setNodeRef: setDropRef }, showValidDropHighlight, showInvalidDropCursor } = useFolderDraggableDroppable(folder);
  const setNodeRef = (node) => {
    setDragRef(node);
    setDropRef(node);
  };
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      navigateToFolder(folder);
    }
  };
  return (0, import_jsx_runtime15.jsxs)(StyledFolderCard, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isValidDropTarget: showValidDropHighlight,
    $isInvalidDropTarget: showInvalidDropCursor,
    onClick: () => navigateToFolder(folder),
    onKeyDown: handleKeyDown,
    role: "listitem",
    tabIndex: 0,
    children: [
      (0, import_jsx_runtime15.jsx)(FolderIconContainer, {
        children: (0, import_jsx_runtime15.jsx)(_5, {
          width: 20,
          height: 20
        })
      }),
      (0, import_jsx_runtime15.jsx)(FolderName, {
        textColor: "neutral800",
        ellipsis: true,
        children: folder.name
      }),
      (0, import_jsx_runtime15.jsx)(be, {
        label: formatMessage({
          id: getTranslationKey("control-card.more-actions"),
          defaultMessage: "More actions"
        }),
        variant: "ghost",
        onClick: (e) => e.stopPropagation(),
        children: (0, import_jsx_runtime15.jsx)(W3, {})
      })
    ]
  });
};
var PreviewContainer2 = gt(S)`
  position: relative;
  width: 100%;
  padding-bottom: 62.5%;
  height: 0;
  overflow: hidden;
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var StyledImage2 = gt.img`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
`;
var IconPreview = gt(v)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral100};
`;
var AssetPreview2 = ({ asset }) => {
  var _a;
  const { alternativeText, ext, formats, mime, url, isLocal, isUrlSigned } = asset;
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    const mediaURL = prefixFileUrlWithBackendUrl((_a = formats == null ? void 0 : formats.thumbnail) == null ? void 0 : _a.url) ?? prefixFileUrlWithBackendUrl(url);
    if (mediaURL) {
      return (0, import_jsx_runtime15.jsx)(PreviewContainer2, {
        children: (0, import_jsx_runtime15.jsx)(StyledImage2, {
          src: mediaURL,
          alt: alternativeText || "",
          // Only signed remote URLs need crossOrigin (cache collision with
          // the preview). Public/unsigned remote thumbnails must render
          // without a bucket CORS rule. See #26581.
          crossOrigin: !isLocal && isUrlSigned ? "anonymous" : void 0,
          draggable: false,
          onDragStart: (e) => e.preventDefault()
        })
      });
    }
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime15.jsx)(PreviewContainer2, {
    children: (0, import_jsx_runtime15.jsx)(IconPreview, {
      justifyContent: "center",
      alignItems: "center",
      children: (0, import_jsx_runtime15.jsx)(DocIcon, {
        width: 48,
        height: 48
      })
    })
  });
};
var StyledCardHeader = gt(Cd)`
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral200};
`;
var CardFooter = gt(v)`
  min-width: 0;
  width: 100%;
`;
var FileTypeIcon = gt(v)`
  color: ${({ theme }) => theme.colors.neutral600};
  flex-shrink: 0;
`;
var FileName = gt(A)`
  flex: 1;
  min-width: 0;
`;
var AssetCard = ({ asset, onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const TypeIcon = getAssetIcon(asset.mime, asset.ext);
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { attributes, listeners, setNodeRef, isDragging } = useFileDraggable(asset);
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    }
  };
  return (0, import_jsx_runtime15.jsxs)(StyledCard, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    tabIndex: 0,
    role: "listitem",
    onDragStart: (e) => e.preventDefault(),
    onClick: () => onAssetItemClick(asset.id),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime15.jsx)(StyledCardHeader, {
        children: (0, import_jsx_runtime15.jsx)(AssetPreview2, {
          asset
        })
      }),
      (0, import_jsx_runtime15.jsx)(xd, {
        children: (0, import_jsx_runtime15.jsxs)(CardFooter, {
          alignItems: "center",
          gap: 2,
          children: [
            (0, import_jsx_runtime15.jsx)(FileTypeIcon, {
              children: (0, import_jsx_runtime15.jsx)(TypeIcon, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime15.jsx)(FileName, {
              textColor: "primary800",
              ellipsis: true,
              children: asset.name
            }),
            (0, import_jsx_runtime15.jsx)(be, {
              label: formatMessage({
                id: getTranslationKey("control-card.more-actions"),
                defaultMessage: "More actions"
              }),
              variant: "ghost",
              onClick: (e) => e.stopPropagation(),
              children: (0, import_jsx_runtime15.jsx)(W3, {})
            })
          ]
        })
      })
    ]
  });
};
var AssetsGrid = ({ assets, folders = [], onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const totalItems = folders.length + assets.length;
  if (totalItems === 0) {
    return (0, import_jsx_runtime15.jsx)(S, {
      padding: 8,
      children: (0, import_jsx_runtime15.jsx)(A, {
        textColor: "neutral600",
        children: formatMessage({
          id: "app.components.EmptyStateLayout.content-document",
          defaultMessage: "No content found"
        })
      })
    });
  }
  return (0, import_jsx_runtime15.jsxs)(Ld.Root, {
    gap: 4,
    role: "list",
    "data-testid": "assets-grid",
    children: [
      folders.length > 0 && (0, import_jsx_runtime15.jsx)(FoldersRow, {
        children: (0, import_jsx_runtime15.jsx)(Ld.Root, {
          gap: 4,
          children: folders.map((folder) => (0, import_jsx_runtime15.jsx)(Ld.Item, {
            col: 3,
            m: 4,
            s: 6,
            xs: 12,
            children: (0, import_jsx_runtime15.jsx)(FolderCard, {
              folder
            })
          }, `folder-${folder.id}`))
        })
      }),
      assets.map((asset) => (0, import_jsx_runtime15.jsx)(Ld.Item, {
        col: 3,
        m: 4,
        s: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime15.jsx)(AssetCard, {
          asset,
          onAssetItemClick
        })
      }, asset.id))
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/constants.mjs
var localStorageKeys = {
  view: `STRAPI_UPLOAD_LIBRARY_VIEW`
};
var viewOptions = {
  GRID: 0,
  TABLE: 1
};
var TABLE_HEADERS = [
  {
    name: "name",
    label: {
      id: getTranslationKey("list.table.header.name"),
      defaultMessage: "name"
    }
  },
  {
    name: "createdAt",
    label: {
      id: getTranslationKey("list.table.header.creationDate"),
      defaultMessage: "creation date"
    }
  },
  {
    name: "updatedAt",
    label: {
      id: getTranslationKey("list.table.header.lastModified"),
      defaultMessage: "last modified"
    }
  },
  {
    name: "size",
    label: {
      id: getTranslationKey("list.table.header.size"),
      defaultMessage: "size"
    }
  },
  {
    name: "actions",
    label: {
      id: getTranslationKey("list.table.header.actions"),
      defaultMessage: "actions"
    },
    isVisuallyHidden: true
  }
];

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var StyledTable = gt(Hl)`
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  border: 1px solid ${({ theme }) => theme.colors.neutral150};
  border-radius: 4px;
  overflow: hidden;
`;
var StyledThead = gt(jl)`
  background: ${({ theme }) => theme.colors.neutral100};

  tr {
    border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
  }
`;
var StyledTh = gt(Vl)`
  height: 40px;
  padding: 0 ${({ theme }) => theme.spaces[4]};
  text-align: left;
`;
var StyledTd = gt(Dn)`
  padding: 0 ${({ theme }) => theme.spaces[4]};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var StyledTr = gt.tr`
  height: 48px;
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: ${({ $isMovePending, $isInvalidDropTarget }) => {
  if ($isMovePending) {
    return "wait";
  }
  return $isInvalidDropTarget ? "not-allowed" : "pointer";
}};
  opacity: ${({ $isDragging }) => $isDragging ? 0.4 : 1};
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      background: ${theme.colors.primary100};
      outline: 1px dashed ${theme.colors.primary600};
      outline-offset: -1px;
    `}

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }

  &:last-child {
    ${StyledTd} {
      border-bottom: 0;
    }
  }
`;
var StyledBodyTd = gt(Dn)`
  padding: ${({ theme }) => theme.spaces[4]};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var AssetPreviewCell = ({ asset }) => {
  const { ext, mime } = asset;
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime16.jsx)(v, {
    justifyContent: "center",
    alignItems: "center",
    borderRadius: "4px",
    color: "neutral500",
    width: "3.2rem",
    height: "3.2rem",
    shrink: 0,
    children: (0, import_jsx_runtime16.jsx)(DocIcon, {
      width: 20,
      height: 20
    })
  });
};
var AssetRow = ({ asset, onAssetItemClick }) => {
  const isMobile = useIsMobile();
  const { formatDate, formatMessage } = useIntl();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { attributes, listeners, setNodeRef, isDragging } = useFileDraggable(asset);
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    }
  };
  return (0, import_jsx_runtime16.jsxs)(StyledTr, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    tabIndex: 0,
    role: "row",
    onDragStart: (e) => e.preventDefault(),
    onClick: () => onAssetItemClick(asset.id),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime16.jsx)(StyledTd, {
        children: (0, import_jsx_runtime16.jsxs)(v, {
          gap: 3,
          alignItems: "center",
          children: [
            (0, import_jsx_runtime16.jsx)(AssetPreviewCell, {
              asset
            }),
            (0, import_jsx_runtime16.jsxs)(v, {
              direction: "column",
              alignItems: "flex-start",
              children: [
                (0, import_jsx_runtime16.jsx)(A, {
                  textColor: "neutral800",
                  fontWeight: "semiBold",
                  ellipsis: true,
                  children: asset.name
                }),
                isMobile && (0, import_jsx_runtime16.jsx)(A, {
                  textColor: "neutral600",
                  variant: "pi",
                  children: asset.size ? formatBytes(asset.size, 1) : "-"
                })
              ]
            })
          ]
        })
      }),
      !isMobile && (0, import_jsx_runtime16.jsxs)(import_jsx_runtime16.Fragment, {
        children: [
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: asset.createdAt ? formatDate(new Date(asset.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: asset.size ? formatBytes(asset.size, 1) : "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime16.jsx)(StyledTd, {
        children: (0, import_jsx_runtime16.jsx)(v, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime16.jsx)(be, {
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            }),
            variant: "ghost",
            onClick: (e) => e.stopPropagation(),
            children: (0, import_jsx_runtime16.jsx)(W3, {})
          })
        })
      })
    ]
  });
};
var FolderTr = gt(StyledTr)`
  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }
`;
var FolderRow = ({ folder }) => {
  const isMobile = useIsMobile();
  const { formatDate, formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { draggable: { attributes, listeners, setNodeRef: setDragRef, isDragging }, droppable: { setNodeRef: setDropRef }, showValidDropHighlight, showInvalidDropCursor } = useFolderDraggableDroppable(folder);
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      navigateToFolder(folder);
    }
  };
  return (0, import_jsx_runtime16.jsxs)(FolderTr, {
    ref: (node) => {
      setDragRef(node);
      setDropRef(node);
    },
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isValidDropTarget: showValidDropHighlight,
    $isInvalidDropTarget: showInvalidDropCursor,
    tabIndex: 0,
    role: "row",
    onDragStart: (e) => e.preventDefault(),
    onClick: () => navigateToFolder(folder),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime16.jsx)(StyledTd, {
        children: (0, import_jsx_runtime16.jsxs)(v, {
          gap: 3,
          alignItems: "center",
          children: [
            (0, import_jsx_runtime16.jsx)(v, {
              justifyContent: "center",
              alignItems: "center",
              borderRadius: "4px",
              color: "neutral600",
              width: "3.2rem",
              height: "3.2rem",
              shrink: 0,
              children: (0, import_jsx_runtime16.jsx)(_5, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral800",
              fontWeight: "semiBold",
              ellipsis: true,
              children: folder.name
            })
          ]
        })
      }),
      !isMobile && (0, import_jsx_runtime16.jsxs)(import_jsx_runtime16.Fragment, {
        children: [
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: folder.createdAt ? formatDate(new Date(folder.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: folder.updatedAt ? formatDate(new Date(folder.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime16.jsx)(StyledTd, {
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime16.jsx)(StyledTd, {
        children: (0, import_jsx_runtime16.jsx)(v, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime16.jsx)(be, {
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            }),
            variant: "ghost",
            onClick: (e) => e.stopPropagation(),
            children: (0, import_jsx_runtime16.jsx)(W3, {})
          })
        })
      })
    ]
  });
};
var AssetsTable = ({ assets, folders = [], onAssetItemClick }) => {
  const isMobile = useIsMobile();
  const { formatMessage } = useIntl();
  const visibleHeaders = isMobile ? TABLE_HEADERS.filter((h) => h.name === "name" || h.name === "actions") : TABLE_HEADERS;
  const totalRows = folders.length + assets.length;
  return (0, import_jsx_runtime16.jsxs)(StyledTable, {
    colCount: visibleHeaders.length,
    rowCount: totalRows + 1,
    children: [
      (0, import_jsx_runtime16.jsx)(StyledThead, {
        children: (0, import_jsx_runtime16.jsx)(Ul, {
          children: visibleHeaders.map((header) => {
            const tableHeaderLabel = formatMessage(header.label);
            const isVisuallyHidden = "isVisuallyHidden" in header && header.isVisuallyHidden;
            if (isVisuallyHidden) {
              return (0, import_jsx_runtime16.jsx)(StyledTh, {
                children: (0, import_jsx_runtime16.jsx)($e, {
                  children: formatMessage({
                    id: getTranslationKey("table.header.actions"),
                    defaultMessage: "actions"
                  })
                })
              }, header.name);
            }
            return (0, import_jsx_runtime16.jsx)(StyledTh, {
              children: (0, import_jsx_runtime16.jsx)(A, {
                textColor: "neutral600",
                variant: "sigma",
                children: tableHeaderLabel
              })
            }, header.name);
          })
        })
      }),
      (0, import_jsx_runtime16.jsx)(_l, {
        children: totalRows === 0 ? (0, import_jsx_runtime16.jsx)(Ul, {
          children: (0, import_jsx_runtime16.jsx)(StyledBodyTd, {
            colSpan: visibleHeaders.length,
            children: (0, import_jsx_runtime16.jsx)(A, {
              textColor: "neutral600",
              children: formatMessage({
                id: "app.components.EmptyStateLayout.content-document",
                defaultMessage: "No content found"
              })
            })
          })
        }) : (0, import_jsx_runtime16.jsxs)(import_jsx_runtime16.Fragment, {
          children: [
            folders.map((folder) => (0, import_jsx_runtime16.jsx)(FolderRow, {
              folder
            }, `folder-${folder.id}`)),
            assets.map((asset) => (0, import_jsx_runtime16.jsx)(AssetRow, {
              asset,
              onAssetItemClick
            }, asset.id))
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/CreateFolderDialog.mjs
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var import_react7 = __toESM(require_react(), 1);
var StyledModalContent = gt(_d.Content)`
  max-width: 51.6rem;
`;
var CreateFolderDialog = ({ open, folderName, parentFolderId, onClose }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const [name, setName] = (0, import_react7.useState)("");
  const [fieldError, setFieldError] = (0, import_react7.useState)();
  const [createFolder, { isLoading }] = useCreateFolderMutation();
  (0, import_react7.useEffect)(() => {
    if (open) {
      setName("");
      setFieldError(void 0);
    }
  }, [
    open
  ]);
  const handleSubmit = async (e) => {
    e.preventDefault();
    const trimmedName = name.trim();
    if (!trimmedName) {
      setFieldError(formatMessage({
        id: getTranslationKey("folder.create.form.error.name-required"),
        defaultMessage: "Name is required"
      }));
      return;
    }
    try {
      await createFolder({
        name: trimmedName,
        parent: parentFolderId
      }).unwrap();
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: getTranslationKey("folder.create.success"),
          defaultMessage: "Folder has been created"
        })
      });
      onClose();
    } catch (err) {
      const apiError = err;
      if (apiError == null ? void 0 : apiError.message) {
        setFieldError(apiError.message);
      } else {
        toggleNotification({
          type: "danger",
          message: formatMessage({
            id: getTranslationKey("folder.create.form.error.unknown"),
            defaultMessage: "An error occurred while creating the folder"
          })
        });
      }
    }
  };
  return (0, import_jsx_runtime17.jsx)(_d.Root, {
    open,
    onOpenChange: onClose,
    children: (0, import_jsx_runtime17.jsxs)(StyledModalContent, {
      children: [
        (0, import_jsx_runtime17.jsx)(_d.Header, {
          children: (0, import_jsx_runtime17.jsx)(_d.Title, {
            children: formatMessage({
              id: getTranslationKey("folder.create.title-in"),
              defaultMessage: "New folder in {folderName}"
            }, {
              folderName
            })
          })
        }),
        (0, import_jsx_runtime17.jsxs)("form", {
          onSubmit: handleSubmit,
          children: [
            (0, import_jsx_runtime17.jsx)(_d.Body, {
              children: (0, import_jsx_runtime17.jsxs)(Dd.Root, {
                error: fieldError,
                name: "name",
                required: true,
                children: [
                  (0, import_jsx_runtime17.jsx)(Dd.Label, {
                    children: formatMessage({
                      id: getTranslationKey("folder.form.name.label"),
                      defaultMessage: "Folder name"
                    })
                  }),
                  (0, import_jsx_runtime17.jsx)(Pc, {
                    value: name,
                    onChange: (e) => {
                      setName(e.target.value);
                      setFieldError(void 0);
                    },
                    autoFocus: true
                  }),
                  (0, import_jsx_runtime17.jsx)(Dd.Error, {})
                ]
              })
            }),
            (0, import_jsx_runtime17.jsx)(_d.Footer, {
              children: (0, import_jsx_runtime17.jsxs)(v, {
                gap: 2,
                justifyContent: "space-between",
                width: "100%",
                children: [
                  (0, import_jsx_runtime17.jsx)(Ct, {
                    variant: "tertiary",
                    onClick: onClose,
                    type: "button",
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  }),
                  (0, import_jsx_runtime17.jsx)(Ct, {
                    type: "submit",
                    loading: isLoading,
                    children: formatMessage({
                      id: getTranslationKey("folder.create.submit"),
                      defaultMessage: "Create folder"
                    })
                  })
                ]
              })
            })
          ]
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var React20 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZoneContext.mjs
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var import_react8 = __toESM(require_react(), 1);
var UploadDropZoneContext = (0, import_react8.createContext)(null);
var DropZoneWrapper = gt(S)`
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 100%;
`;
var UploadDropZoneProvider = ({ children, onDrop }) => {
  const [isDragging, setIsDragging] = (0, import_react8.useState)(false);
  const dragCounterRef = (0, import_react8.useRef)(0);
  const contextValue = {
    isDragging
  };
  (0, import_react8.useEffect)(() => {
    const handleDragEnd = () => {
      setIsDragging(false);
      dragCounterRef.current = 0;
    };
    const handleDocumentDragLeave = (e) => {
      if (!e.relatedTarget) {
        setIsDragging(false);
        dragCounterRef.current = 0;
      }
    };
    document.addEventListener("dragend", handleDragEnd);
    document.addEventListener("dragleave", handleDocumentDragLeave);
    return () => {
      document.removeEventListener("dragend", handleDragEnd);
      document.removeEventListener("dragleave", handleDocumentDragLeave);
    };
  }, []);
  const handleDragEnter = (0, import_react8.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    if (!e.dataTransfer.types.includes("Files")) {
      return;
    }
    dragCounterRef.current += 1;
    setIsDragging(true);
  }, []);
  const handleDragLeave = (0, import_react8.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current -= 1;
    if (dragCounterRef.current <= 0) {
      setIsDragging(false);
      dragCounterRef.current = 0;
    }
  }, []);
  const handleDragOver = (0, import_react8.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    e.dataTransfer.dropEffect = "copy";
  }, []);
  const handleDrop = (0, import_react8.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragging(false);
    dragCounterRef.current = 0;
    const { files } = e.dataTransfer;
    if ((files == null ? void 0 : files.length) && onDrop) {
      onDrop(Array.from(files));
    }
  }, [
    onDrop
  ]);
  return (0, import_jsx_runtime18.jsx)(UploadDropZoneContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime18.jsx)(DropZoneWrapper, {
      "data-testid": "assets-dropzone",
      onDragEnter: handleDragEnter,
      onDragLeave: handleDragLeave,
      onDragOver: handleDragOver,
      onDrop: handleDrop,
      children
    })
  });
};
var useUploadDropZone = () => {
  const context = (0, import_react8.useContext)(UploadDropZoneContext);
  if (!context) {
    throw new Error("useUploadDropZone must be used within UploadDropZone");
  }
  return {
    isDragging: context.isDragging
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var setOpacity = (hex, alpha) => `${hex}${Math.floor(alpha * 255).toString(16).padStart(2, "0")}`;
var DropZoneOverlay = gt(S)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: ${({ theme }) => setOpacity(theme.colors.primary200, 0.3)};
  border: 1px solid ${({ theme }) => theme.colors.primary700};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 1;
  pointer-events: none;
`;
var DropZoneWithOverlay = ({ children }) => {
  const { isDragging: isUploadDragActive } = useUploadDropZone();
  const assetsDnd = useAssetsDndOptional();
  const isInternalDragActive = (assetsDnd == null ? void 0 : assetsDnd.isInternalDragActive) ?? false;
  const showOverlay = isUploadDragActive && !isInternalDragActive;
  return (0, import_jsx_runtime19.jsxs)(S, {
    position: "relative",
    children: [
      showOverlay && (0, import_jsx_runtime19.jsx)(DropZoneOverlay, {}),
      children
    ]
  });
};
var DropFilesMessageImpl = gt(S)`
  position: fixed;
  bottom: ${({ theme }) => theme.spaces[8]};
  left: 50%;
  transform: translateX(calc(-50% + ${({ $leftContentWidth }) => $leftContentWidth / 2}px));
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: ${({ theme }) => theme.spaces[2]};
  background: ${({ theme }) => theme.colors.primary600};
  padding: ${({ theme }) => theme.spaces[4]} ${({ theme }) => theme.spaces[6]};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 2;
`;
var DropFilesMessage = ({ uploadDropZoneRef, folderName }) => {
  const { formatMessage } = useIntl();
  const { isDragging: isUploadDragActive } = useUploadDropZone();
  const assetsDnd = useAssetsDndOptional();
  const isInternalDragActive = (assetsDnd == null ? void 0 : assetsDnd.isInternalDragActive) ?? false;
  const showMessage = isUploadDragActive && !isInternalDragActive;
  const [leftContentWidth, setLeftContentWidth] = React20.useState(0);
  React20.useEffect(() => {
    if (!(uploadDropZoneRef == null ? void 0 : uploadDropZoneRef.current)) return;
    const updateRect = () => {
      var _a;
      const rect = (_a = uploadDropZoneRef.current) == null ? void 0 : _a.getBoundingClientRect();
      if (rect) {
        setLeftContentWidth((prev) => prev !== rect.left ? rect.left : prev);
      }
    };
    updateRect();
    const resizeObserver = new ResizeObserver(updateRect);
    resizeObserver.observe(uploadDropZoneRef.current);
    return () => resizeObserver.disconnect();
  }, [
    uploadDropZoneRef
  ]);
  if (!showMessage) {
    return null;
  }
  return (0, import_jsx_runtime19.jsxs)(DropFilesMessageImpl, {
    $leftContentWidth: leftContentWidth,
    children: [
      (0, import_jsx_runtime19.jsx)(A, {
        textColor: "neutral0",
        children: formatMessage({
          id: getTranslationKey("dropzone.upload.message"),
          defaultMessage: "Drop here to upload to"
        })
      }),
      (0, import_jsx_runtime19.jsxs)(v, {
        gap: 2,
        alignItems: "center",
        children: [
          (0, import_jsx_runtime19.jsx)(_5, {
            width: 20,
            height: 20,
            fill: "neutral0"
          }),
          (0, import_jsx_runtime19.jsx)(A, {
            textColor: "neutral0",
            fontWeight: "semiBold",
            children: folderName
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderTree/FolderTree.mjs
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var import_react9 = __toESM(require_react(), 1);
var RowButton = gt.button`
  display: flex;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  width: 100%;
  min-height: 3.2rem;
  padding: ${({ theme }) => `${theme.spaces[1]} ${theme.spaces[2]}`};
  border: 0;
  background: ${({ $isActive, theme }) => $isActive ? theme.colors.primary100 : "transparent"};
  color: ${({ $isActive, theme }) => $isActive ? theme.colors.primary700 : theme.colors.neutral800};
  border-radius: ${({ theme }) => theme.borderRadius};
  cursor: pointer;
  text-align: left;
  font: inherit;

  &:hover {
    background: ${({ $isActive, theme }) => $isActive ? theme.colors.primary100 : theme.colors.neutral100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var findAncestorIds = (nodes, targetId, trail = []) => {
  var _a;
  for (const node of nodes) {
    if (node.id === targetId) {
      return trail;
    }
    if ((_a = node.children) == null ? void 0 : _a.length) {
      const nextTrail = node.id != null ? [
        ...trail,
        node.id
      ] : trail;
      const found = findAncestorIds(node.children, targetId, nextTrail);
      if (found !== null) {
        return found;
      }
    }
  }
  return null;
};
var useExpandedFolders = (folderStructure, currentFolderId) => {
  const [expandedIds, setExpandedIds] = (0, import_react9.useState)(() => /* @__PURE__ */ new Set());
  (0, import_react9.useEffect)(() => {
    if (currentFolderId == null) {
      return;
    }
    const ancestors = findAncestorIds(folderStructure, currentFolderId);
    if (!ancestors || ancestors.length === 0) {
      return;
    }
    setExpandedIds((prev) => {
      const next = new Set(prev);
      let changed = false;
      for (const id of ancestors) {
        if (!next.has(id)) {
          next.add(id);
          changed = true;
        }
      }
      return changed ? next : prev;
    });
  }, [
    folderStructure,
    currentFolderId
  ]);
  const toggleExpanded = (0, import_react9.useCallback)((id) => {
    setExpandedIds((prev) => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  }, []);
  const isExpanded = (0, import_react9.useCallback)((id) => expandedIds.has(id), [
    expandedIds
  ]);
  return {
    isExpanded,
    toggleExpanded
  };
};
var TruncatedFolderName = ({ name, isActive }) => {
  const textRef = (0, import_react9.useRef)(null);
  const [isTruncated, setIsTruncated] = (0, import_react9.useState)(false);
  (0, import_react9.useLayoutEffect)(() => {
    const el = textRef.current;
    if (!el) {
      return;
    }
    const checkTruncation = () => {
      setIsTruncated(el.scrollWidth > el.clientWidth);
    };
    checkTruncation();
    const observer = new ResizeObserver(checkTruncation);
    observer.observe(el);
    return () => observer.disconnect();
  }, [
    name
  ]);
  const label = (0, import_jsx_runtime20.jsx)(A, {
    ref: textRef,
    variant: "omega",
    fontWeight: isActive ? "semiBold" : "regular",
    ellipsis: true,
    children: name
  });
  if (isTruncated) {
    return (0, import_jsx_runtime20.jsx)(Mt, {
      label: name,
      children: label
    });
  }
  return label;
};
var NavList = gt.ul`
  list-style: none;
  margin: 0;
  padding: 0;
`;
var INDENT_PER_LEVEL_REM = 1.6;
var RotatingChevron = gt(o5)`
  transform: rotate(${({ $expanded }) => $expanded ? "0deg" : "-90deg"});
  transition: transform 0.2s ease;
`;
var FolderTreeItem = ({ node, level, currentFolderId, isExpanded, onToggle, onSelect }) => {
  var _a;
  const { formatMessage } = useIntl();
  if (node.id == null) {
    return null;
  }
  const id = node.id;
  const name = node.name ?? "";
  const hasChildren = (((_a = node.children) == null ? void 0 : _a.length) ?? 0) > 0;
  const isFolderExpanded = isExpanded(id);
  const isActive = currentFolderId === id;
  return (0, import_jsx_runtime20.jsxs)("li", {
    children: [
      (0, import_jsx_runtime20.jsxs)(v, {
        alignItems: "center",
        paddingLeft: `${level * INDENT_PER_LEVEL_REM}rem`,
        gap: 1,
        children: [
          (0, import_jsx_runtime20.jsx)(be, {
            label: formatMessage({
              id: getTranslationKey(isFolderExpanded ? "sidebar.tree.collapse" : "sidebar.tree.expand"),
              defaultMessage: isFolderExpanded ? "Collapse {name}" : "Expand {name}"
            }, {
              name
            }),
            onClick: (event) => {
              event.stopPropagation();
              onToggle(id);
            },
            variant: "ghost",
            withTooltip: false,
            "aria-expanded": isFolderExpanded,
            children: (0, import_jsx_runtime20.jsx)(RotatingChevron, {
              $expanded: isFolderExpanded,
              fill: "neutral500"
            })
          }),
          (0, import_jsx_runtime20.jsx)(S, {
            flex: "1",
            minWidth: 0,
            children: (0, import_jsx_runtime20.jsx)(RowButton, {
              type: "button",
              $isActive: isActive,
              "aria-current": isActive ? "page" : void 0,
              onClick: () => onSelect(id),
              "data-testid": `folder-tree-node-${id}`,
              "data-folder-id": id,
              children: (0, import_jsx_runtime20.jsx)(TruncatedFolderName, {
                name,
                isActive
              })
            })
          })
        ]
      }),
      hasChildren && isFolderExpanded && (0, import_jsx_runtime20.jsx)(NavList, {
        children: node.children.map((child) => (0, import_jsx_runtime20.jsx)(FolderTreeItem, {
          node: child,
          level: level + 1,
          currentFolderId,
          isExpanded,
          onToggle,
          onSelect
        }, child.id ?? child.name))
      })
    ]
  });
};
var SidebarNav = gt(v)`
  /* TODO: reconcile 25.6rem (Figma) with admin WIDTH_SIDE_NAVIGATION (23.2rem) */
  width: 25.6rem;
  height: 100%;
  min-height: 100%;
  background: ${({ theme }) => theme.colors.neutral0};
  flex-shrink: 0;
  flex-direction: column;
  border-right: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var SidebarHeader = gt(S)`
  flex-shrink: 0;
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var SidebarBody = gt(v)`
  flex: 1;
  min-height: 0;
  overflow-y: auto;
`;
var FolderTree = ({ currentFolderId, onSelectFolder }) => {
  const { formatMessage } = useIntl();
  const { data: folderStructure = [], isLoading, isError } = useGetFolderStructureQuery();
  const { isExpanded, toggleExpanded } = useExpandedFolders(folderStructure, currentFolderId);
  const isHomeActive = currentFolderId == null;
  return (0, import_jsx_runtime20.jsxs)(SidebarNav, {
    direction: "column",
    alignItems: "stretch",
    tag: "nav",
    "aria-label": formatMessage({
      id: getTranslationKey("sidebar.tree.aria-label"),
      defaultMessage: "Media library folders"
    }),
    children: [
      (0, import_jsx_runtime20.jsx)(SidebarHeader, {
        paddingTop: 4,
        paddingBottom: 4,
        paddingLeft: 5,
        paddingRight: 5,
        children: (0, import_jsx_runtime20.jsx)(A, {
          variant: "beta",
          tag: "h2",
          children: formatMessage({
            id: getTranslationKey("sidebar.title"),
            defaultMessage: "Media library"
          })
        })
      }),
      (0, import_jsx_runtime20.jsxs)(SidebarBody, {
        direction: "column",
        alignItems: "stretch",
        gap: 1,
        padding: 3,
        children: [
          (0, import_jsx_runtime20.jsxs)(RowButton, {
            type: "button",
            $isActive: isHomeActive,
            "aria-current": isHomeActive ? "page" : void 0,
            onClick: () => onSelectFolder(null),
            "data-testid": "folder-tree-home",
            children: [
              (0, import_jsx_runtime20.jsx)(d3, {
                "aria-hidden": true,
                width: "1.6rem",
                height: "1.6rem"
              }),
              (0, import_jsx_runtime20.jsx)(A, {
                variant: "omega",
                fontWeight: isHomeActive ? "semiBold" : "regular",
                children: formatMessage({
                  id: getTranslationKey("sidebar.home"),
                  defaultMessage: "Home"
                })
              })
            ]
          }),
          (0, import_jsx_runtime20.jsxs)(S, {
            children: [
              (0, import_jsx_runtime20.jsxs)(v, {
                alignItems: "center",
                gap: 1,
                padding: 1,
                children: [
                  (0, import_jsx_runtime20.jsx)(_5, {
                    "aria-hidden": true,
                    width: "1.6rem",
                    height: "1.6rem",
                    fill: "neutral500"
                  }),
                  (0, import_jsx_runtime20.jsx)(A, {
                    variant: "sigma",
                    textColor: "neutral600",
                    style: {
                      textTransform: "uppercase"
                    },
                    children: formatMessage({
                      id: getTranslationKey("sidebar.folders"),
                      defaultMessage: "Folders"
                    })
                  })
                ]
              }),
              isLoading ? (
                // TODO: revisit loading state before revamp GA
                (0, import_jsx_runtime20.jsx)(v, {
                  justifyContent: "center",
                  padding: 1,
                  paddingTop: 2,
                  children: (0, import_jsx_runtime20.jsx)(kt, {
                    children: formatMessage({
                      id: getTranslationKey("sidebar.tree.loading"),
                      defaultMessage: "Loading folders..."
                    })
                  })
                })
              ) : isError ? (
                // TODO: revisit error state before revamp GA
                (0, import_jsx_runtime20.jsx)(S, {
                  padding: 1,
                  paddingTop: 2,
                  children: (0, import_jsx_runtime20.jsx)(A, {
                    variant: "pi",
                    textColor: "danger600",
                    children: formatMessage({
                      id: getTranslationKey("sidebar.tree.error"),
                      defaultMessage: "Could not load folders."
                    })
                  })
                })
              ) : folderStructure.length === 0 ? (
                // TODO: revisit empty state before revamp GA
                (0, import_jsx_runtime20.jsx)(S, {
                  padding: 1,
                  paddingTop: 2,
                  children: (0, import_jsx_runtime20.jsx)(A, {
                    variant: "pi",
                    textColor: "neutral500",
                    children: formatMessage({
                      id: getTranslationKey("sidebar.tree.empty"),
                      defaultMessage: "No folders yet"
                    })
                  })
                })
              ) : (0, import_jsx_runtime20.jsx)(NavList, {
                children: folderStructure.map((node) => (0, import_jsx_runtime20.jsx)(FolderTreeItem, {
                  node,
                  level: 0,
                  currentFolderId,
                  isExpanded,
                  onToggle: toggleExpanded,
                  onSelect: onSelectFolder
                }, node.id ?? node.name))
              })
            ]
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/ImportFromUrlDialog.mjs
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var React21 = __toESM(require_react(), 1);
var ImportFromUrlDialog = ({ open, onClose, onUpload }) => {
  const { formatMessage } = useIntl();
  const [urls, setUrls] = React21.useState("");
  const [error, setError] = React21.useState(null);
  const handleClose = () => {
    setUrls("");
    setError(null);
    onClose();
  };
  const handleSubmit = async (e) => {
    e.preventDefault();
    const { urls: validUrls, error: validationError } = validateUrls(urls);
    if (validationError) {
      setError(validationError);
      return;
    }
    setError(null);
    handleClose();
    await onUpload(validUrls);
  };
  return (0, import_jsx_runtime21.jsx)(_d.Root, {
    open,
    onOpenChange: (isOpen) => !isOpen && handleClose(),
    children: (0, import_jsx_runtime21.jsx)(_d.Content, {
      children: (0, import_jsx_runtime21.jsxs)("form", {
        onSubmit: handleSubmit,
        children: [
          (0, import_jsx_runtime21.jsx)(_d.Header, {
            children: (0, import_jsx_runtime21.jsx)(_d.Title, {
              children: formatMessage({
                id: getTranslationKey("modal.url.title"),
                defaultMessage: "Import from URL"
              })
            })
          }),
          (0, import_jsx_runtime21.jsx)(_d.Body, {
            children: (0, import_jsx_runtime21.jsxs)(Dd.Root, {
              error: error || void 0,
              hint: formatMessage({
                id: getTranslationKey("input.url.description"),
                defaultMessage: "Separate your URL links by a carriage return."
              }),
              children: [
                (0, import_jsx_runtime21.jsx)(Dd.Label, {
                  children: formatMessage({
                    id: getTranslationKey("input.url.label"),
                    defaultMessage: "URL(s)"
                  })
                }),
                (0, import_jsx_runtime21.jsx)($u, {
                  name: "urls",
                  minHeight: "unset",
                  rows: Math.min(urls.split("\n").length, 7),
                  maxHeight: "10.5rem",
                  placeholder: formatMessage({
                    id: getTranslationKey("input.url.placeholder"),
                    defaultMessage: "Empty"
                  }),
                  value: urls,
                  onChange: (e) => {
                    setUrls(e.target.value);
                    setError(null);
                  }
                }),
                (0, import_jsx_runtime21.jsx)(Dd.Hint, {}),
                (0, import_jsx_runtime21.jsx)(Dd.Error, {})
              ]
            })
          }),
          (0, import_jsx_runtime21.jsxs)(_d.Footer, {
            children: [
              (0, import_jsx_runtime21.jsx)(Ct, {
                variant: "tertiary",
                onClick: handleClose,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime21.jsx)(Ct, {
                type: "submit",
                children: formatMessage({
                  id: getTranslationKey("modal.url.upload"),
                  defaultMessage: "Upload"
                })
              })
            ]
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useInfiniteAssets.mjs
var import_react10 = __toESM(require_react(), 1);
var PAGE_SIZE = 20;
var useInfiniteAssets = ({ folder = null, sort } = {}) => {
  const [page, setPage] = (0, import_react10.useState)(1);
  const lastResultsRef = (0, import_react10.useRef)([]);
  const isMountRef = (0, import_react10.useRef)(true);
  const { currentData: data, isLoading, isFetching, error } = useGetAssetsQuery({
    folder,
    page,
    pageSize: PAGE_SIZE,
    sort
  });
  const pagination = data == null ? void 0 : data.pagination;
  const assets = (0, import_react10.useMemo)(() => {
    if (!data) {
      return lastResultsRef.current;
    }
    const currentPageResults = data.results;
    if (page === 1) {
      lastResultsRef.current = currentPageResults;
    } else {
      const expectedPrior = (page - 1) * PAGE_SIZE;
      if (lastResultsRef.current.length < expectedPrior - PAGE_SIZE) {
        return lastResultsRef.current;
      }
      if (lastResultsRef.current.length < page * PAGE_SIZE) {
        lastResultsRef.current = [
          ...lastResultsRef.current,
          ...currentPageResults
        ];
      }
    }
    return lastResultsRef.current;
  }, [
    data,
    page
  ]);
  (0, import_react10.useEffect)(() => {
    if (isMountRef.current) {
      isMountRef.current = false;
      return;
    }
    setPage(1);
    lastResultsRef.current = [];
  }, [
    folder,
    sort
  ]);
  const hasNextPage = pagination ? page < pagination.pageCount : false;
  const isFetchingMore = isFetching && page > 1;
  const fetchNextPage = (0, import_react10.useCallback)(() => {
    setPage((prev) => prev + 1);
  }, []);
  return {
    assets,
    pagination,
    isLoading,
    isFetchingMore,
    hasNextPage,
    fetchNextPage,
    error
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var INTERSECTION_OPTIONS = {
  threshold: 0.1
};
var AssetsView = ({ view, folderId, onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const { assets, isLoading: isLoadingAssets, isFetchingMore, hasNextPage, fetchNextPage, error } = useInfiniteAssets({
    folder: folderId
  });
  const { data: folders = [], isLoading: isLoadingFolders } = useGetFoldersQuery({
    parentId: folderId
  });
  const isGridView = view === viewOptions.GRID;
  const isLoading = isLoadingAssets || isLoadingFolders;
  const loadMoreRef = useElementOnScreen((0, import_react11.useCallback)((isVisible) => {
    if (isVisible && hasNextPage && !isFetchingMore) {
      fetchNextPage();
    }
  }, [
    hasNextPage,
    isFetchingMore,
    fetchNextPage
  ]), INTERSECTION_OPTIONS);
  if (isLoading) {
    return (0, import_jsx_runtime22.jsx)(v, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime22.jsx)(kt, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error) {
    return (0, import_jsx_runtime22.jsx)(S, {
      padding: 8,
      children: (0, import_jsx_runtime22.jsx)(A, {
        textColor: "danger600",
        children: formatMessage({
          id: getTranslationKey("list.assets.error"),
          defaultMessage: "An error occurred while fetching assets."
        })
      })
    });
  }
  if (folders.length === 0 && assets.length === 0) {
    return (0, import_jsx_runtime22.jsx)(S, {
      padding: 8,
      children: (0, import_jsx_runtime22.jsx)(A, {
        textColor: "neutral600",
        children: formatMessage({
          id: "app.components.EmptyStateLayout.content-document",
          defaultMessage: "No content found"
        })
      })
    });
  }
  return (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, {
    children: [
      isGridView ? (0, import_jsx_runtime22.jsx)(AssetsGrid, {
        folders,
        assets,
        onAssetItemClick
      }) : (0, import_jsx_runtime22.jsx)(AssetsTable, {
        assets,
        folders,
        onAssetItemClick
      }),
      (0, import_jsx_runtime22.jsx)("div", {
        ref: loadMoreRef,
        style: {
          height: 1
        }
      }),
      isFetchingMore && (0, import_jsx_runtime22.jsx)(v, {
        justifyContent: "center",
        padding: 4,
        children: (0, import_jsx_runtime22.jsx)(kt, {
          children: formatMessage({
            id: getTranslationKey("list.assets.loading-more"),
            defaultMessage: "Loading more assets..."
          })
        })
      })
    ]
  });
};
var StyledToggleGroup = gt(Root2)`
  display: flex;
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  overflow: hidden;
`;
var StyledToggleItem = gt(Item2)`
  display: flex;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[4]}`};
  border: none;
  background: ${({ theme }) => theme.colors.neutral0};
  color: ${({ theme }) => theme.colors.neutral800};
  cursor: pointer;
  font-size: ${({ theme }) => theme.fontSizes[1]};
  font-weight: ${({ theme }) => theme.fontWeights.semiBold};

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &[data-state='on'] {
    background: ${({ theme }) => theme.colors.neutral150};
  }

  svg {
    width: 1.6rem;
    height: 1.6rem;
  }
`;
var HeaderWrapper = gt(S)`
  [data-strapi-header] {
    background: ${({ theme }) => theme.colors.neutral0};

    h1 {
      font-size: 1.8rem;
    }
  }
`;
var AssetsPage = () => {
  const { formatMessage } = useIntl();
  const { openDetails } = useAssetDetailsParam();
  const { currentFolderId, navigateToFolderId, navigateToRoot } = useFolderNavigation();
  const { error: currentFolderError } = useGetFolderQuery({
    id: currentFolderId
  }, {
    skip: currentFolderId === null
  });
  (0, import_react11.useEffect)(() => {
    if ((currentFolderError == null ? void 0 : currentFolderError.name) === "NotFoundError") {
      navigateToRoot();
    }
  }, [
    currentFolderError,
    navigateToRoot
  ]);
  const { title, itemCount } = useFolderInfo(currentFolderId);
  const itemCountLabel = formatMessage({
    id: getTranslationKey("header.content.item-count"),
    defaultMessage: "{count, plural, =1 {# item} other {# items}}"
  }, {
    count: itemCount
  });
  const pageHeaderTitle = title ? `${title} (${itemCountLabel})` : formatMessage({
    id: "app.loading",
    defaultMessage: "Loading..."
  });
  const [isCreateFolderDialogOpen, setIsCreateFolderDialogOpen] = (0, import_react11.useState)(false);
  const [view, setView] = usePersistentState(localStorageKeys.view, viewOptions.GRID);
  const isGridView = view === viewOptions.GRID;
  const [isUrlDialogOpen, setIsUrlDialogOpen] = (0, import_react11.useState)(false);
  const fileInputRef = (0, import_react11.useRef)(null);
  const uploadDropZoneRef = (0, import_react11.useRef)(null);
  const [uploadFiles] = useUploadFilesMutation();
  const [uploadFromUrls] = useUploadFromUrlsMutation();
  const uploadFilesToFolder = async (files, folderId) => {
    if (files.length === 0) return;
    const formData = new FormData();
    const fileInfoArray = [];
    files.forEach((file) => {
      formData.append("files", file);
      fileInfoArray.push({
        name: file.name,
        caption: null,
        alternativeText: null,
        folder: folderId
      });
    });
    formData.append("fileInfo", JSON.stringify(fileInfoArray));
    try {
      await uploadFiles({
        formData,
        totalFiles: files.length
      }).unwrap();
    } catch (error) {
    }
  };
  const handleFileSelect = () => {
    var _a;
    (_a = fileInputRef.current) == null ? void 0 : _a.click();
  };
  const handleFileChange = async (e) => {
    const files = e.target.files;
    if (files && files.length > 0) {
      await uploadFilesToFolder(Array.from(files), currentFolderId);
    }
    e.target.value = "";
  };
  const handleDrop = async (files) => {
    await uploadFilesToFolder(files, currentFolderId);
  };
  const handleUrlUpload = async (urls) => {
    try {
      await uploadFromUrls({
        urls,
        folderId: currentFolderId
      }).unwrap();
    } catch (error) {
    }
  };
  return (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, {
    children: [
      (0, import_jsx_runtime22.jsx)(UploadDropZoneProvider, {
        onDrop: handleDrop,
        children: (0, import_jsx_runtime22.jsx)(AssetsDndProvider, {
          children: (0, import_jsx_runtime22.jsx)(S, {
            ref: uploadDropZoneRef,
            children: (0, import_jsx_runtime22.jsxs)(Layouts.Root, {
              minHeight: "100vh",
              background: "neutral0",
              sideNav: (0, import_jsx_runtime22.jsx)(FolderTree, {
                currentFolderId,
                onSelectFolder: navigateToFolderId
              }),
              children: [
                (0, import_jsx_runtime22.jsx)($e, {
                  children: (0, import_jsx_runtime22.jsx)("input", {
                    type: "file",
                    ref: fileInputRef,
                    onChange: handleFileChange,
                    multiple: true
                  })
                }),
                (0, import_jsx_runtime22.jsx)(HeaderWrapper, {
                  children: (0, import_jsx_runtime22.jsx)(Layouts.Header, {
                    title: pageHeaderTitle,
                    primaryAction: (0, import_jsx_runtime22.jsxs)(ti, {
                      popoverPlacement: "bottom-end",
                      variant: "default",
                      endIcon: (0, import_jsx_runtime22.jsx)(o5, {}),
                      label: formatMessage({
                        id: getTranslationKey("new"),
                        defaultMessage: "New"
                      }),
                      children: [
                        (0, import_jsx_runtime22.jsx)(pd, {
                          onSelect: () => setIsCreateFolderDialogOpen(true),
                          startIcon: (0, import_jsx_runtime22.jsx)(_5, {}),
                          children: formatMessage({
                            id: getTranslationKey("folder.create.title"),
                            defaultMessage: "New folder"
                          })
                        }),
                        (0, import_jsx_runtime22.jsx)(pd, {
                          onSelect: handleFileSelect,
                          startIcon: (0, import_jsx_runtime22.jsx)(J5, {}),
                          children: formatMessage({
                            id: getTranslationKey("import-files"),
                            defaultMessage: "Import files"
                          })
                        }),
                        (0, import_jsx_runtime22.jsx)(pd, {
                          onSelect: () => setIsUrlDialogOpen(true),
                          startIcon: (0, import_jsx_runtime22.jsx)(R3, {}),
                          children: formatMessage({
                            id: getTranslationKey("import-from-url"),
                            defaultMessage: "Import from URL"
                          })
                        })
                      ]
                    }),
                    subtitle: (0, import_jsx_runtime22.jsxs)(v, {
                      justifyContent: "space-between",
                      alignItems: "center",
                      gap: 4,
                      width: "100%",
                      children: [
                        (0, import_jsx_runtime22.jsx)(v, {
                          gap: 4,
                          alignItems: "center",
                          children: "TODO: Filters and search"
                        }),
                        (0, import_jsx_runtime22.jsxs)(v, {
                          gap: 4,
                          alignItems: "center",
                          children: [
                            (0, import_jsx_runtime22.jsx)(S, {
                              children: "TODO: Sort"
                            }),
                            (0, import_jsx_runtime22.jsxs)(StyledToggleGroup, {
                              type: "single",
                              value: isGridView ? "grid" : "table",
                              onValueChange: (value) => value && setView(value === "grid" ? viewOptions.GRID : viewOptions.TABLE),
                              "aria-label": formatMessage({
                                id: getTranslationKey("view.switch.label"),
                                defaultMessage: "View options"
                              }),
                              children: [
                                (0, import_jsx_runtime22.jsxs)(StyledToggleItem, {
                                  value: "table",
                                  "aria-label": formatMessage({
                                    id: getTranslationKey("view.table"),
                                    defaultMessage: "Table view"
                                  }),
                                  children: [
                                    (0, import_jsx_runtime22.jsx)(B3, {}),
                                    formatMessage({
                                      id: getTranslationKey("view.table"),
                                      defaultMessage: "Table view"
                                    })
                                  ]
                                }),
                                (0, import_jsx_runtime22.jsxs)(StyledToggleItem, {
                                  value: "grid",
                                  "aria-label": formatMessage({
                                    id: getTranslationKey("view.grid"),
                                    defaultMessage: "Grid view"
                                  }),
                                  children: [
                                    (0, import_jsx_runtime22.jsx)(t3, {}),
                                    formatMessage({
                                      id: getTranslationKey("view.grid"),
                                      defaultMessage: "Grid view"
                                    })
                                  ]
                                })
                              ]
                            })
                          ]
                        })
                      ]
                    })
                  })
                }),
                (0, import_jsx_runtime22.jsx)(Layouts.Content, {
                  children: (0, import_jsx_runtime22.jsxs)(DropZoneWithOverlay, {
                    children: [
                      (0, import_jsx_runtime22.jsx)(DropFilesMessage, {
                        uploadDropZoneRef,
                        folderName: title
                      }),
                      (0, import_jsx_runtime22.jsx)(AssetsView, {
                        view,
                        folderId: currentFolderId,
                        onAssetItemClick: openDetails
                      })
                    ]
                  })
                })
              ]
            })
          })
        })
      }),
      (0, import_jsx_runtime22.jsx)(CreateFolderDialog, {
        open: isCreateFolderDialogOpen,
        folderName: title,
        parentFolderId: currentFolderId,
        onClose: () => setIsCreateFolderDialogOpen(false)
      }),
      (0, import_jsx_runtime22.jsx)(ImportFromUrlDialog, {
        open: isUrlDialogOpen,
        onClose: () => setIsUrlDialogOpen(false),
        onUpload: handleUrlUpload
      }),
      (0, import_jsx_runtime22.jsx)(AssetDetailsDrawer, {})
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var UnstableMediaLibrary = () => {
  const { formatMessage } = useIntl();
  const title = formatMessage({
    id: getTranslationKey("plugin.name"),
    defaultMessage: "Media Library"
  });
  return (0, import_jsx_runtime23.jsxs)(Page.Main, {
    children: [
      (0, import_jsx_runtime23.jsx)(Page.Title, {
        children: title
      }),
      (0, import_jsx_runtime23.jsx)(Routes, {
        children: (0, import_jsx_runtime23.jsx)(Route, {
          index: true,
          element: (0, import_jsx_runtime23.jsx)(AssetsPage, {})
        })
      })
    ]
  });
};
export {
  UnstableMediaLibrary
};
//# sourceMappingURL=App-WOTVQDZL.js.map
