import { ADMIN_VITE_ALIAS_MODULES } from './admin-vite-alias-modules.mjs';
import { getModulePath } from './resolve-module.mjs';

/**
 * Vite resolve.alias entries for the admin bundle — every path comes from @strapi/admin's closure.
 *
 * @internal
 */ const buildAdminViteResolveAliases = ()=>Object.fromEntries(ADMIN_VITE_ALIAS_MODULES.map((mod)=>[
            mod,
            getModulePath(mod)
        ]));

export { buildAdminViteResolveAliases };
//# sourceMappingURL=admin-vite-aliases.mjs.map
